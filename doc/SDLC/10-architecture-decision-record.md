# 10. Architecture Decision Record - Hệ thống đặt vé xe khách

## 1. Thông tin tài liệu

### 1.1. Metadata

| Thuộc tính   | Giá trị                          |
| ------------ | -------------------------------- |
| Tên tài liệu | Architecture Decision Record - ADR |
| Mã tài liệu  | 10-architecture-decision-record  |
| Dự án        | Hệ thống đặt vé xe khách         |
| Phiên bản    | v0.21                            |
| Trạng thái   | Draft                            |
| Người viết   | Nguyễn Hồng Khanh, AI Agent          |
| Người duyệt  | Nguyễn Hồng Khanh                |
| Ngày cập nhật | 01/06/2026 |
| Ngày tạo     | 11/05/2026                       |

### 1.2. Lịch sử thay đổi

| Phiên bản | Ngày       | Người cập nhật | Nội dung thay đổi                         |
| --------- | ---------- | -------------- | ----------------------------------------- |
| v0.1      | 11/05/2026 | AI Agent       | Tạo bản nháp ADR                          |
| v0.2      | 25/05/2026 | AI Agent       | Rebrand sang `Marketplace-Ve-Xe-Nhanh`; cập nhật ADR-003 nguồn (TECH-STACK file đã xóa → chỉ còn HLD)  |
| v0.3      | 25/05/2026 | AI Agent       | Sprint 0 Layer 1 workshop output: refine ADR-002 sang framework-agnostic "Modular monolith" + bổ sung scalability ceiling + alternatives đã xét + mitigation. Mark ADR-003 (Mongo), ADR-004 (Redis), ADR-007 (Expo Mobile) sang **Pending re-decision** do 5 OQ vendor re-opened (xem PROJECT-STATE §4). Không thay đổi ADR-001, 005, 006, 008. |
| v0.4      | 25/05/2026 | AI Agent       | Sprint 0 Layer 2 workshop output: thêm **ADR-009 Backend language + runtime = TypeScript + Node.js LTS 22.x**. Matrix 7 candidate (TS+Node / TS+Bun / Go / Python+FastAPI / Elixir+Phoenix / Rust+Axum / Java-Kotlin) × 8 axes. Khanh self-rate "Mạnh TS/JS, yếu ngôn ngữ khác" + chốt TS+Node. Không thay đổi ADR-001..008. |
| v0.5      | 25/05/2026 | AI Agent       | Sprint 0 Layer 3 workshop output (Sprint 0 closing layer): thêm **ADR-010 Backend framework = NestJS 11 + nestjs-zod**. Matrix 6 candidate (NestJS / Fastify / Hono / Express / AdonisJS / Koa+tsoa) × 9 axes. Khanh chốt NestJS với 2 tinh chỉnh: nestjs-zod thay class-validator + ESLint rule enforce DOMAIN-MAP §1, §2 module boundary. **Sprint 0 closed** — 3/3 layer foundation hoàn tất (Architecture / BE language / BE framework). Không thay đổi ADR-001..009. |
| v0.6      | 25/05/2026 | AI Agent       | Sprint 1 Layer 4 workshop output: thêm **ADR-011 DB paradigm = Hybrid-A (Postgres 16 + Prisma 5 cho operational, MongoDB 7 + Mongoose cho audit/log)**. Matrix 5 paradigm × 10 axes. Khanh chốt Hybrid-A + Prisma 5 + Khanh self-rate "Mạnh cả SQL + NoSQL". **Supersede ADR-003** (Mongo là operational DB chính — no longer true). **Re-close OQ-14** với wording cập nhật (Mongo cluster RIÊNG, không phải same cluster như closure 11/05/2026). OQ-15 reporting implementation refined (Postgres CTE + materialized view cho structured report; Mongo aggregation cho audit query). ADR-004 (Redis) và ADR-007 (Expo) vẫn Pending. |
| v0.7      | 25/05/2026 | AI Agent       | Sprint 1 Layer 5 workshop output: thêm **ADR-012 API style = REST + OpenAPI 3.1 auto (Zod single source)**. Matrix 5 candidate (REST+OpenAPI / GraphQL / tRPC / gRPC / Hybrid REST+tRPC) × 10 axes. Khanh chốt REST. Toolchain: nestjs-zod auto-gen OpenAPI 3.1 → `openapi-typescript` + (`orval`/`@hey-api/openapi-ts`) gen client. URL versioning `/v1/*`. RFC 7807 error format. Webhook HMAC signature. 5 mitigation (type infer codegen step, boilerplate, HTTP verb mapping complex action, OpenAPI 3.1 vs 3.0 gap, webhook security). **Unblock 05-API Spec**. Không thay đổi ADR khác. |
| v0.8      | 25/05/2026 | AI Agent       | Sprint 1 Layer 6 workshop output: thêm **ADR-013 Frontend framework = Next.js 16 App Router + Turborepo monorepo (pnpm workspace)**. Matrix 6 candidate (Next / Remix / Vite+RR7 / SvelteKit / Astro / TanStack Start) × 11 axes + monorepo vs multi-repo comparison 13 axes. Khanh chốt Next + Monorepo. Folder structure `apps/api+marketplace+operator-os+admin + packages/types+api-client+ui+utils+config`. App count cụ thể (3 app vs 2 app gộp back-office) defer LLD Sprint 5-6. Component lib (Shadcn/ui recommend, alternatives Mantine/AntD/Chakra) defer Sprint 5-6. 7 mitigation (RSC overhead, bundle, carryover trap, mobile share, code review surface, build time, permission boundary). Không thay đổi ADR khác. |
| v0.9      | 25/05/2026 | AI Agent       | Sprint 1 Layer 7 workshop output (Sprint 1 closing layer): thêm **ADR-014 Mobile framework = Expo (managed) SDK 55+ React Native, 2 app split (Passenger + Employee)**. Matrix 6 candidate (Expo managed / RN bare / Flutter / Capacitor+Web / PWA only / Native Swift+Kotlin) × 14 axes. Khanh chốt Expo managed + 2-app split (apps/passenger-mobile + apps/employee-mobile cùng monorepo, share packages/types + api-client + ui-mobile-shared). **Supersede ADR-007** (Expo single codebase actor-split — no longer accurate, 2-app split thay). **Re-close OQ-10** với wording cập nhật (Expo managed + 2-app split, không phải single codebase như closure cũ 10/05/2026). 7 mitigation (perf bridge, permission profile khác per app, ASO marketing tách, EAS Build 2 project, deep link payment, push token per app, eject path nếu cần native module). **Sprint 1 closed** — 4/4 layer Phase 1 Foundation hoàn tất (DB / API / Frontend / Mobile). Phase 2 Infrastructure mở khóa Sprint 2. |
| v0.10     | 26/05/2026 | AI Agent       | Sprint 2 Layer 8 workshop output: thêm **ADR-015 Cache + Distributed Lock = Redis 7 (Upstash managed SG)**. Matrix 4 paradigm (A. Redis cho cả / B. Pure Postgres / C. Hybrid pg-lock+Redis-cache / D. Defer 1-instance) × 12 axes. Workshop có concept primer + use case mapping vì Khanh nghiệp dư cache/lock. Khanh chốt A + Upstash managed SG. Seat hold v1 implementation = Pure Redis `SET NX EX 600`; Hybrid (Redis + Postgres `seat_hold` table) defer LLD Sprint 5-6 nếu race condition đo được. **Supersede ADR-004** (Redis carryover legacy — refined với vendor cụ thể + scope cache+lock only; Layer 9 queue cùng dùng Redis sẽ là ADR-016). 6 mitigation (Redis down → 503 không fallback in-memory, vendor lock Upstash → standard protocol ioredis, cache invalidation TTL-based, cost monitoring, ops learning curve, seat hold race condition). CLAUDE.md §2 thêm Cache+Lock row. Sprint 2 còn 3 layer: Queue (9), Auth (10), Storage (11). |
| v0.11     | 26/05/2026 | AI Agent       | Sprint 2 Layer 9 workshop output: thêm **ADR-016 Async Job Queue = BullMQ + @nestjs/bullmq trên Redis Upstash (cùng Layer 8)**. Matrix 4 candidate (A. BullMQ / B. pg-boss / C. Temporal / D. Cloud queue defer) × 12 axes. Workshop có concept primer + 10 use case marketplace mapping vì Khanh nghiệp dư. Khanh chốt A. Worker deployment v1 **defer Sprint 4 (Phase 4 DevOps)** — tạm in-process; refactor sang separate worker process khi container/deploy target chốt. Workflow engine cho KYC OCR không cần v1 (manual admin review chính). 7 mitigation (Redis SPoF leverage ADR-015 fallback, outbox pattern best-effort v1, long-running > 30 min stalled job heartbeat, dashboard Bull Board mount /admin/queues, idempotency dedup by job ID, cron payout T+3 lock single worker via BullMQ rate limiter, Strangler → Temporal post-v1 nếu workflow demand). CLAUDE.md §2 thêm Queue row. Sprint 2 còn 2 layer: Auth (10), Storage (11). |
| v0.12     | 26/05/2026 | AI Agent       | Sprint 2 Layer 10 workshop output (2 vòng): thêm **ADR-017 Auth library + Identity model = Better Auth + custom NestJS adapter, Account-separate per scope, Platform-controlled provisioning**. **Vòng 1**: 5 candidate (Better Auth / Auth.js v5 / Passport+NestJS / Clerk / Supabase Auth — Lucia v3 loại deprecated end-2024) × 14 axes + 3 sub-decision (auth lib / token model / OAuth). Khanh "[No preference]" cả 3 → AI áp dụng Recommended defaults (Better Auth + Hybrid JWT 15min access + opaque refresh 30 ngày rotation+family invalidation + Defer OAuth Sprint 3 Phase 3 Layer 13). **Vòng 2**: Khanh challenge model "Email = identifier Operator" raise 3 vấn đề (1 Operator có nhiều owner, login identifier Platform-controlled, Platform Admin tương tự). Reframe Entity/Identity/Account 3 namespace tách biệt. Khanh chốt **Q1=A** (`{operatorSlug}/{username}` slash separator), **Q2=α** (Account separate v1 không federate), **Q3=A** (Closed enrollment Platform-controlled, Operator/Admin không self-register). Identifier 3 namespace: Passenger=Email; Operator-side=`{slug}/{username}`; Platform-side=`platform/{username}`. Provisioning: Platform admin issue Operator slug + first Owner; Owner issue employee; super-admin issue Platform employee. Token Hybrid JWT RS256 15min + opaque refresh 30d rotation+family invalidation. RBAC 8 role v1, ABAC defer. Multi-tenant guard TenantGuard + Postgres RLS defense-in-depth. CLAUDE.md §2 thêm Auth row. Sprint 2 còn 1 layer: Storage (11). **CRITICAL re-confirm khi 07 Security §5/§6 + LLD Sprint 5-6 rework**. |
| v0.13     | 26/05/2026 | AI Agent       | Sprint 2 Layer 11 workshop output (Sprint 2 closing layer): thêm **ADR-018 Object Storage = Cloudflare R2 + Storage adapter (KYC dev-local / production-deferred)**. Workshop có concept primer (object storage vs DB/filesystem, bucket/object/key/presigned URL/CDN/lifecycle/encryption) + use case mapping 8 loại file × public/private/sensitivity + compliance flag (Nghị định 13/2023 PDPD cross-border DPIA + 53/2022 data residency). Matrix 5 option (Cloudflare R2 / AWS S3 / Backblaze B2 / VN local / MinIO) × 12 axes. Khanh chốt **Q1=A** Cloudflare R2 provider chính (egress $0, S3-compatible `@aws-sdk/client-s3`, CDN built-in, ops thấp); 2 bucket public/private + presigned URL TTL 5min + KYC download audit. **Q2**: KYC + payment-proof dev/test = local storage adapter (filesystem/MinIO, không data thật); **production = defer phát triển tiếp** (VN local split-bucket vs R2+DPIA) — blocker trước go-live, track OQ-21 + ADR-OQ-04 (sửa 31/05/2026: trùng ID với ADR-OQ-03 Node/Docker pinning có sẵn → đánh lại OQ-04). Storage adapter pattern consistent ADR-006. 7 mitigation (KYC prod OQ-21 blocker, R2 nước ngoài non-KYC PII, vendor lock S3-compat, local dev parity MinIO, presigned leak TTL+audit, egress fair-use monitor, 2 provider adapter abstraction). **Close SEC-OQ-04**. CLAUDE.md §2 thêm Object Storage row. **Sprint 2 CLOSED — 4/4 layer Phase 2 Infrastructure (Redis Upstash / BullMQ / Better Auth / R2). Phase 3 VN vendor (Sprint 3) mở khóa.** |
| v0.14     | 31/05/2026 | AI Agent       | Sprint 3 Layer 12 workshop output (Sprint 3 opening layer, Phase 3 VN vendor): thêm **ADR-019 Payment Gateway = VNPay primary + MoMo phương thức 2 (đa phương thức v1), sau `PaymentGateway` adapter port `external/payment/{vnpay,momo}/`**. Workshop có concept primer (redirect/hosted vs API-direct → PCI SAQ-A vì cổng VN đều hosted; IPN/webhook nguồn chân lý + verify HMAC + idempotent; querydr/reconcile; settlement về merchant account hợp escrow in-house) + cờ pháp lý critical (escrow = hoạt động trung gian thanh toán theo Nghị định 52/2024 cần NHNN license cho production). Matrix 5 option (VNPay / MoMo / ZaloPay / OnePay / Stripe) + note VietQR (PayOS/Casso/SePay) × 7 axes (VN coverage / DX-AI assist / sandbox time-to-v1 / refund API / escrow fit / phí / khuyến nghị). Khanh chốt **Q2 = VNPay + MoMo đa phương thức v1**; **Q1 (primary) + Q3 (raise OQ TGTT) trao AI áp recommendation** → Q1=A VNPay primary (incumbent AS-08, napas ~40 bank + QR + thẻ quốc tế phủ rộng nhất 1 tích hợp), Q3=A raise **OQ-22 + ADR-OQ-05** (giấy phép TGTT NHNN production blocker, KHÔNG block MVP). Stack: `PaymentGateway` port + `VnpayAdapter` HMAC-SHA512 + `MomoAdapter` HMAC-SHA256 per ADR-006/AS-08. Webhook async qua BullMQ Layer 9 + verify HMAC + dedup `(provider, providerTxnId)` idempotent (FR-BTP-08, NFR-DATA-04, NFR-PERF-04). Refund API riêng từng cổng (OQ-13). Reconciliation scheduled BullMQ cron call querydr mỗi cổng đối chiếu DB (FR-BTP-17, NFR-DATA-03). Escrow giữ in-house `EscrowLedger` per ADR-005 không đổi (cổng chỉ thu hộ về merchant account Platform). Stripe loại v1 (không onboarding merchant VN, không settle VND OQ-12); VietQR defer v1.x option phương thức 3. 8 mitigation (giấy phép TGTT OQ-22 production blocker, 2 webhook HMAC khác adapter giấu, downtime querydr reconcile BullMQ + flag RECONCILING, phí 1.1-2%+ ảnh hưởng commission 5% OQ-18 → net sau phí trong `CommissionRule`, vendor lock adapter abstraction, MoMo onboarding song song VNPay từ LLD Sprint 5-6, refund API điều kiện HĐ + manual escalate admin, **CRITICAL re-confirm Sprint 5-6** do Q1+Q3 AI tự áp recommendation tương tự ADR-017 pattern). **Re-close OQ-05** với wording cập nhật (VNPay primary + MoMo phương thức 2 v1, không phải single VNPay Sandbox như closure cũ 11/05/2026). Raise **OQ-22 + ADR-OQ-05 mới**. CLAUDE.md §2 thêm Payment Gateway row + Stack row Sprint 3 progress 1/4. Sprint 3 còn 3 layer: Notification (13) / Routing (14) / Bank payout (15). |
| v0.15     | 31/05/2026 | AI Agent       | Sprint 3 Layer 13 workshop output: thêm **ADR-020 Notification (Email + SMS + Push) + OAuth provider v1**. 4 sub-decision matrix (Email 7 option × 5 axes / SMS 5 option × 5 axes + 2 hướng α-defer/β-chốt-sớm / Push 5 option × 5 axes / OAuth 5 provider × 4 axes) + primer (SPF/DKIM/DMARC, Brandname MIC, FCM/APNs/Expo Push routing, OAuth PKCE + account linking + App Store Guideline 4.8) + compliance flag (Resend cross-border PII NĐ 13/2023 standard, SMS Brandname MIC defer-ready, Push token PII auth_session standard, OAuth privacy disclosure). Khanh **chốt qua tiêu chí "miễn phí cho v1"** → AI áp recommendation: **Q1=A Resend** Email primary (free 3k/mo, React Email templates fit Next.js Layer 6, swap path SendGrid/AWS SES nếu vượt), **Q2=α defer SMS v1** (chỉ `SmsNotifier` port + LocalLoggerAdapter dev; production không enable; v1.x activate candidate eSMS.vn), **Q3=A Expo Push Service** (native ADR-014 không eject, `expo-server-sdk-node` route FCM+APNs, free always), **Q4=A Google + Facebook + Apple v1 cho Passenger + Zalo defer v1.x** (Better Auth built-in OAuth providers, Apple Sign-In mandatory App Store Guideline 4.8, Apple Dev $99/yr sunk cost ADR-014, Zalo cần custom adapter + Zalo OA KYC nặng audience overlap FB). Operator/Platform KHÔNG OAuth per ADR-017 (closed enrollment `{scope}/{username}`). Fan-out architecture: business event → BullMQ Layer 9 job `notify-fan-out` → 3 adapter song song (email + push + sms-noop) → audit Mongo `notification_log` per ADR-011 + retry policy + DLQ. Account linking Better Auth tự handle (OAuth email match Email-OTP merge). 8 mitigation (Resend 3k/mo cap monitor + swap SendGrid/SES, deliverability DKIM/SPF + magic-link fallback + rate-limit OTP SEC-OQ-07, không SMS v1 risk → adapter ready + activate v1.x khi đo OTP fail rate, Expo Push fair-use monitor + FCM Direct migration path, Apple Sign-In cert setup 1 lần LLD, OAuth account linking edge case Better Auth UI manual link, Zalo defer audience track conversion post-MVP, **CRITICAL re-confirm Sprint 5-6 do Khanh chốt qua tiêu chí không direct per-Q selection — tương tự ADR-017 + ADR-019 pattern**). **Re-close OQ-09** với wording cập nhật (Email Resend primary + SMS adapter defer + Expo Push + OAuth Google/Facebook/Apple Passenger-only, không phải chỉ "Email OTP + SMS adapter future" như closure cũ 11/05/2026). CLAUDE.md §2 thêm Notification + OAuth row + Stack row Sprint 3 progress 2/4. Sprint 3 còn 2 layer: Routing (14) / Bank payout (15). |
| v0.16     | 31/05/2026 | AI Agent       | Sprint 3 Layer 14 workshop output: thêm **ADR-021 Routing Engine + Map/Geocoding = Mapbox (Matrix API + GL) free tier + OSRM self-host escape-hatch**. **Strategic reframe**: marketplace vé xe tuyến cố định KHÔNG ride-hailing → routing là dependency mức "assist" (tính distance/duration giữa stop-point lúc config route/trip → cache DB, gợi ý điểm đón/trả geo proximity, map display marker), KHÔNG turn-by-turn/dispatch realtime (SRS dòng 346 tối ưu AI + 350 IoT GPS/OBD ngoài phạm vi v1) → volume API cực thấp → free tier dư → KHÔNG cần self-host 24/7. 2 sub-decision matrix (Routing 6 option: Mapbox/ORS/Google/OSRM-self-host/GraphHopper/Valhalla × 6 axes / Map+Geocoding 4 option: Mapbox GL/Leaflet+OSM/Google/MapTiler) + primer (routing-matrix vs map-tiles-geocoding tách biệt, self-host vs hosted ops trade-off, cache-once pattern) + carryover trap flag (OSRM là default legacy DP-07 + DOMAIN-MAP `external/routing/osrm/` — đánh giá lại fresh không rubber-stamp). Khanh chốt **A + B theo khuyến nghị** (giữ tiêu chí "miễn phí cho v1"): **Q1=A Mapbox Matrix/Directions API** (free 100k req/mo, zero-ops) sau `RoutingProvider` adapter, distance/duration cache DB; **Q2=A Mapbox GL** cho map tiles + geocoding (free 50k loads/mo, cùng access token Sub A, Web `mapbox-gl` + Mobile `@rnmapbox/maps`); **Q3=A OSRM self-host = documented escape-hatch** (KHÔNG build v1; swap adapter khi volume/residency demand sau). Tọa độ stop-point = địa điểm công cộng KHÔNG PII → data residency N/A → **KHÔNG raise OQ mới**. Adapter port `external/routing/` mandated per ADR-006/NFR-COMP-05. 7 mitigation (free tier cap monitor + cache DB giảm call + swap, vendor lock Mapbox → adapter code chỉ `external/routing/mapbox/`, OSRM escape-hatch documented DOMAIN-MAP chừa chỗ, Mapbox token env config free tier không cần thẻ ban đầu, map VN quality OSM-based + stop-point catalog chuẩn có tọa độ + manual pick override, Mobile `@rnmapbox/maps` Expo config plugin setup 1 lần Sprint 4, distance/duration stale cache refresh periodic). **Không re-close OQ** (OQ-16 bank payout là Layer 15). Re-confirm nhẹ khi LLD Sprint 5-6 nếu volume vượt free tier — KHÔNG gắn CRITICAL như ADR-017/019/020 vì low-risk assist-level + adapter-swappable. CLAUDE.md §2 thêm Routing row + Stack row Sprint 3 progress 3/4. Sprint 3 còn 1 layer: Bank payout (15) re-close OQ-16. |
| v0.17     | 01/06/2026 | AI Agent       | Sprint 3 Layer 15 workshop output (Sprint 3 closing layer, Phase 3 VN vendor): thêm **ADR-022 Bank Payout Channel = Manual admin-confirm + batch export sau `PayoutProvider` adapter; auto-disbursement (VNPay/MoMo chi hộ) defer v1.x**. Vế CHI escrow đối xứng Layer 12 (vế THU). SRS đã định hướng (OQ-16 closure dòng 328 manual confirm v1 + dòng 430 bên thứ ba khi mở rộng) → không over-defer, khớp lại SRS + thêm adapter. Matrix 4 option (Manual admin-confirm / Auto VNPay-MoMo chi hộ / Bank direct API / Third-party disbursement) × axes (tích hợp / phí / MVP fit / phụ thuộc OQ-22 / khớp SRS) + primer (payout flow escrow eligible − commission − refund − adjustment − holdback per dòng 570; 3 cơ chế chuyển tiền manual/auto-API/bank-direct; liên hệ OQ-22 TGTT "chi cho Operator"). Khanh chốt **Q1 = Manual admin-confirm + batch export** + giữ tiêu chí "miễn phí cho v1" cho Q2 (auto-disbursement roadmap = VNPay/MoMo chi hộ reuse Layer 12, tied OQ-22) + Q3 (maker-checker dual-control = documented mitigation). Flow v1: BullMQ cron T+3 concurrency 1 (ADR-016) gom escrow eligible → `Payout` PENDING (trừ commission OQ-18 + refund + adjustment) → admin review batch → tự chuyển khoản tới `BankAccount` verified (KYC OQ-19) → nhập mã giao dịch ngân hàng → `Payout` COMPLETED + `ReconciliationRecord` (DP-18). CO-27 audit who/why/when; AS-23 payout policy versioning. `PayoutProvider` port `external/payout/` mandated per ADR-006; v1 = `ManualPayoutAdapter`. **KHÔNG raise OQ mới** (OQ-22 Layer 12 đã cover payout legal "chi cho Operator"). 7 mitigation (manual không scale → batch export bulk + adapter swap auto v1.x, manual payout fraud single admin → maker-checker documented RBAC PlatformAdmin/Support, real-money depends OQ-22 → sandbox không vướng + auto chi hộ là hướng (b) OQ-22, bank txn ref nhập tay sai → validation + reconciliation DP-18 + Operator confirm FR-OPR-07, BankAccount đổi → re-verify + admin approve + audit, T+3 cron lỗi/trùng → BullMQ concurrency 1 + idempotent per (operator, period), payout policy áp ngược → AS-23 versioning + snapshot commission rate). Re-confirm nhẹ khi LLD nếu auto-disbursement bật (tied OQ-22) — KHÔNG CRITICAL vì Q1 chốt trực tiếp khớp SRS OQ-16 + low-risk. **Re-close OQ-16** với wording cập nhật (Manual admin-confirm + batch export sau adapter + auto-disbursement defer v1.x, giữ tinh thần closure cũ + thêm adapter abstraction). CLAUDE.md §2 thêm Bank Payout row + Stack row Phase 3 CLOSED. **Sprint 3 CLOSED — 4/4 layer Phase 3 VN vendor (Payment VNPay+MoMo / Notification Resend+Expo+OAuth / Routing Mapbox / Payout manual). Toàn bộ 5 OQ vendor re-opened (OQ-05/09/10/14/16) đã re-closed — KHÔNG còn OQ vendor block MVP. Phase 4 DevOps (Sprint 4) mở khóa.** |
| v0.18     | 01/06/2026 | AI Agent       | Sprint 4 Layer 16 workshop output (Phase 4 DevOps opening layer): thêm **ADR-023 Deploy target = Render managed PaaS (Singapore) cho Node API + BullMQ worker; DB managed-separate (Postgres Supabase/Neon SG + Mongo Atlas SG per ADR-011); Docker portable + GitHub auto-deploy**. Reframe: phần lớn stack đã managed (Redis Upstash SG / R2 Cloudflare / SaaS Mapbox/Resend/Expo/VNPay/MoMo) → deploy target chỉ cần host Node API + worker. Matrix 4 option (Render / Railway / Fly.io / VPS Docker Compose) + VN cloud defer × axes (ops nghiệp dư / cost / worker+cron fit / region VN / Docker / DB managed / CI-CD / scaling / time-to-v1). Khanh chốt **Render (Recommended)** — native service type Web + Background Worker + Cron Job khớp thẳng BullMQ worker (Layer 17) + payout T+3 cron, ops thấp, SG region, free tier + ~$7/service, Docker portable. 6 mitigation (data residency KYC/payment production → OQ-21/22 nhưng MVP dev-local không vướng, free tier cold-start → paid API always-on, vendor lock → Docker+env standard swap Fly/VPS, DB managed-separate Supabase/Atlas backup tốt hơn Render addon, cost theo số service → monitor, region SG ~30-50ms acceptable). Influences Layer 17 (worker = Render Background Worker). **Unblock 09 Deploy** (partial). Phase 4 còn 3 layer: Worker deployment (17) / Test framework (18) / Monitoring+CI-CD (19). |
| v0.19     | 01/06/2026 | AI Agent       | Sprint 4 Layer 17 workshop output: thêm **ADR-024 Worker deployment = BullMQ worker tách thành Render Background Worker service riêng** (cùng Docker image với API, khác start command `main.ts`/`worker.ts`). **Resolve worker deployment defer ADR-016** (deploy target Render đã chốt = trigger refactor). Payout T+3 cron = BullMQ repeatable job `0 0 * * *` concurrency 1 trong worker (giữ ADR-016, không Render Cron Job riêng vì worker always-on). Matrix: tách (chọn) vs in-process vs Render Cron Job riêng vs worker repo riêng. Khanh chốt **Tách (Render Background Worker)** — heavy job (payout/reporting) off API event loop + scale worker độc lập + Render native cùng image setup tối thiểu. 5 mitigation (+1 service cost monitor, 2 process init guard main/worker tách, worker crash → Redis AOF + auto-restart + stalled detection, cron trùng → concurrency 1 + idempotent (operator,period), deploy lệch version → cùng image cùng commit). Consistent ADR-002/016/023/015/022. Phase 4 còn 2 layer: Test framework (18) / Monitoring+CI-CD (19). |
| v0.20     | 01/06/2026 | AI Agent       | Sprint 4 Layer 18 workshop output: thêm **ADR-025 Test framework = Vitest (unit+integration BE NestJS + FE Next, 1 runner monorepo) + Supertest (e2e API) + Playwright (e2e web) + Maestro (e2e mobile Expo)**. Test pyramid primer + stack mapping. Mandatory test: money BIGINT/Decimal (ADR-009/011), idempotency payment/seat-hold (ADR-015/019), tenant isolation RLS (ADR-011/017), webhook HMAC dedup (ADR-019). Integration DB = Testcontainers/compose (chốt LLD); contract test Pact/Dredd vs OpenAPI defer. Matrix: Vitest vs Jest (unit) + Maestro vs Detox (mobile); Supertest + Playwright chung. Khanh chốt **Vitest + Supertest + Playwright + Maestro** (1 runner monorepo nhanh ESM + Maestro đơn giản fit Expo solo). Tất cả free OSS. 5 mitigation (Vitest+NestJS ít battle-test → swap Jest dễ cùng API, Maestro < Detox → đủ flow chính + swap, integration DB chậm CI → tách unit/integration, mobile e2e flaky → retry + critical path, coverage ưu tiên money/idempotency/tenant/security). ADR file 10 bump v0.19 → v0.20: thêm **ADR-025**. **Unblock 08 Test Plan**. Phase 4 còn 1 layer: Monitoring+CI-CD (19). |
| v0.21     | 01/06/2026 | AI Agent       | Sprint 4 Layer 19 workshop output (Sprint 4 closing layer, Phase 4 DevOps): thêm **ADR-026 Monitoring + CI/CD = GitHub Actions + Render auto-deploy + Sentry-centric observability**. CI/CD: GitHub Actions (ESLint + tsc + Vitest ADR-025 + build + gen api-client openapi-typescript ADR-012 + Turborepo affected) → merge main → Render auto-deploy (ADR-023 cùng image API+worker) + EAS Build mobile (ADR-014). Observability: Sentry (error+perf+trace phủ BE NestJS + FE Next + Mobile Expo 1 tool) + Pino structured log → Render logs + Render metric built-in + OpenTelemetry vendor-neutral (swap Grafana/Datadog dễ) + free uptime monitor. Full Grafana/Prometheus/Loki defer (ops nặng solo); Datadog/New Relic paid loại v1. Matrix 3 option (Sentry-centric / Grafana Cloud stack / Minimal) + CI/CD GitHub Actions chung + primer log/metric/trace/error/CI-CD. Khanh chốt **Sentry-centric** (1 tool cả stack, ops thấp, free tier) + tiêu chí "miễn phí v1". 6 mitigation (Sentry free cap → sample+filter, Render logs ephemeral → Pino JSON ship aggregator khi cần, metric hạn chế → Sentry perf+OTel, không full trace v1 → OTel sẵn bật Tempo sau, CI/CD secret → GH secrets + Render env, CI chậm → Turborepo affected + tách e2e job). ADR file 10 bump v0.20 → v0.21: thêm **ADR-026**. **Unblock 09 Deploy (full) + 11 Task Breakdown**. **Sprint 4 CLOSED — 4/4 Phase 4 DevOps layer (Deploy Render / Worker tách / Test Vitest+Maestro / Monitoring Sentry+GitHub Actions). TOÀN BỘ 19 layer roadmap (15 tech-selection + 4 DevOps) CHỐT XONG. Còn 2 production-blocker OQ-21 (KYC storage) + OQ-22 (TGTT license).** |

---

## 2. Mục lục

1. Thông tin tài liệu
2. Mục lục
3. Quy ước ADR
4. Danh sách quyết định
5. ADR chi tiết
6. Open Questions / TBD

---

## 3. Quy ước ADR

| Trường | Ý nghĩa |
| ------ | ------- |
| ID | Mã quyết định `ADR-NNN` |
| Trạng thái | Proposed / Accepted / Superseded / Deprecated |
| Bối cảnh | Vấn đề cần quyết định |
| Quyết định | Phương án được chọn |
| Hệ quả | Tác động tích cực/tiêu cực |
| Nguồn | SRS/HLD/OQ liên quan |

AI Agent không tự chuyển ADR sang `Accepted` nếu chưa có người duyệt xác nhận.

---

## 4. Danh sách quyết định

| ID | Tiêu đề | Trạng thái | Nguồn |
| -- | ------- | ---------- | ----- |
| ADR-001 | Managed marketplace là mô hình sản phẩm v1 | Proposed | SRS MQ-05 |
| ADR-002 | Architectural pattern v1: Modular monolith (framework-agnostic) | Proposed | Sprint 0 Layer 1 (25/05/2026); HLD-DEC-01 (replaced) |
| ADR-003 | MongoDB là operational database chính | **Superseded by ADR-011** (25/05/2026) | Tech re-select Layer 4 — paradigm pick = Hybrid (Postgres ops + Mongo audit), Mongo no longer operational chính |
| ADR-004 | Redis dùng cho cache, queue và seat hold support | **Superseded by ADR-015** (26/05/2026) | Tech re-select Layer 8 — refined vendor cụ thể (Upstash managed SG) + scope cache+lock; Layer 9 queue sẽ là ADR-016 cùng dùng Redis |
| ADR-005 | Payment flow dùng escrow trước payout Operator | Proposed | SRS MQ-01 |
| ADR-006 | Payment/notification/storage dùng adapter boundary | Proposed | HLD-DEC-03 |
| ADR-007 | Mobile dùng một codebase Expo cho User và Employee | **Superseded by ADR-014** (25/05/2026) | Tech re-select Layer 7 — chuyển sang 2-app split (Passenger + Employee), single codebase actor-split không còn áp dụng |
| ADR-008 | Admin là arbiter cuối cùng trong dispute | Proposed | SRS MQ-03 |
| ADR-009 | Backend language + runtime v1: TypeScript + Node.js LTS 22.x | Proposed | Sprint 0 Layer 2 (25/05/2026) |
| ADR-010 | Backend framework v1: NestJS 11 + nestjs-zod | Proposed | Sprint 0 Layer 3 (25/05/2026) |
| ADR-011 | DB paradigm v1: Hybrid-A (Postgres 16 + Prisma 5 ops, Mongo 7 audit) | Proposed | Sprint 1 Layer 4 (25/05/2026); supersedes ADR-003 |
| ADR-012 | API style v1: REST + OpenAPI 3.1 auto (Zod single source) | Proposed | Sprint 1 Layer 5 (25/05/2026) |
| ADR-013 | Frontend framework v1: Next.js 16 App Router + Turborepo monorepo | Proposed | Sprint 1 Layer 6 (25/05/2026) |
| ADR-014 | Mobile framework v1: Expo (managed) SDK 55+ React Native, 2 app split | Proposed | Sprint 1 Layer 7 (25/05/2026); supersedes ADR-007 |
| ADR-015 | Cache + Distributed Lock v1: Redis 7 (Upstash managed SG) cho cache + lock | Proposed | Sprint 2 Layer 8 (26/05/2026); supersedes ADR-004 |
| ADR-016 | Async Job Queue v1: BullMQ + @nestjs/bullmq trên Redis Upstash (cùng Layer 8) | Proposed | Sprint 2 Layer 9 (26/05/2026) |
| ADR-017 | Auth library + identity model v1: Better Auth + custom NestJS adapter, Account-separate per scope, Platform-controlled provisioning | Proposed | Sprint 2 Layer 10 (26/05/2026) |
| ADR-018 | Object Storage v1: Cloudflare R2 + Storage adapter (KYC dev-local / production-deferred) | Proposed | Sprint 2 Layer 11 (26/05/2026); closes SEC-OQ-04 |
| ADR-019 | Payment Gateway v1: VNPay primary + MoMo phương thức 2 (đa phương thức v1), sau `PaymentGateway` adapter | Proposed | Sprint 3 Layer 12 (31/05/2026); re-closes OQ-05 |
| ADR-020 | Notification (Email + SMS + Push) + OAuth provider v1: Resend Email / SMS adapter-port defer / Expo Push / Google+Facebook+Apple OAuth Passenger-only (Zalo defer v1.x) — toàn bộ tier free / no marginal cost | Proposed | Sprint 3 Layer 13 (31/05/2026); re-closes OQ-09 |
| ADR-021 | Routing Engine + Map/Geocoding v1: Mapbox (Matrix API + GL) free tier zero-ops sau `RoutingProvider` adapter; OSRM self-host = documented escape-hatch; distance/duration cache DB | Proposed | Sprint 3 Layer 14 (31/05/2026) |
| ADR-022 | Bank Payout Channel v1: Manual admin-confirm + batch export sau `PayoutProvider` adapter; auto-disbursement (VNPay/MoMo chi hộ) defer v1.x tied OQ-22 | Proposed | Sprint 3 Layer 15 (01/06/2026); re-closes OQ-16; closes Sprint 3 |
| ADR-023 | Deploy target v1: Render managed PaaS (SG) cho Node API + BullMQ worker; DB managed-separate (Postgres Supabase/Neon + Mongo Atlas SG); Docker + GitHub auto-deploy | Proposed | Sprint 4 Layer 16 (01/06/2026); Phase 4 DevOps; unblock 09 Deploy |
| ADR-024 | Worker deployment v1: BullMQ worker tách thành Render Background Worker service riêng (cùng image, khác start command); payout cron = BullMQ repeatable concurrency 1; resolve defer ADR-016 | Proposed | Sprint 4 Layer 17 (01/06/2026); Phase 4 DevOps; resolves ADR-016 worker defer |
| ADR-025 | Test framework v1: Vitest (unit+integration BE+FE monorepo) + Supertest (e2e API) + Playwright (e2e web) + Maestro (e2e mobile Expo); mandatory test money/idempotency/tenant-RLS | Proposed | Sprint 4 Layer 18 (01/06/2026); Phase 4 DevOps; unblock 08 Test |
| ADR-026 | Monitoring + CI/CD v1: GitHub Actions (lint/typecheck/Vitest/build/gen-client + Turborepo affected) + Render auto-deploy; Observability = Sentry-centric (error+perf+trace BE+FE+Mobile) + Pino logs + OpenTelemetry vendor-neutral | Proposed | Sprint 4 Layer 19 (01/06/2026); Phase 4 DevOps closing; unblock 09 Deploy + 11 Task; closes Sprint 4 |

---

## 5. ADR chi tiết

### ADR-001. Managed marketplace là mô hình sản phẩm v1

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed |
| Bối cảnh | Hệ thống cần xác định rõ Platform là marketplace, Operator là đơn vị vận tải, User là khách mua vé. |
| Quyết định | V1 theo mô hình managed marketplace gồm Marketplace layer, Operator OS layer và Platform admin layer. |
| Hệ quả | Platform cần KYC, escrow, payout, dispute, audit, data isolation; không trực tiếp vận hành xe. |
| Nguồn | SRS MQ-05, SRS §4-6 |

### ADR-002. Architectural pattern v1: Modular monolith (framework-agnostic)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (refined 25/05/2026 — Sprint 0 Layer 1 workshop output) |
| Bối cảnh | `Marketplace-Ve-Xe-Nhanh` v1 là dự án mới, solo+AI team. Sau quyết định full tech re-select (25/05/2026), kiến trúc cần chốt **trước** khi chọn framework/DB/cache. Workload v1 ước lượng <1,000 concurrent peak với headroom 50-100x trên single-instance monolith. Money flow (escrow → commission → payout) cần in-process transaction để tránh saga pattern phức tạp. Multi-tenant qua `operatorId` cần single tenant filter layer. |
| Quyết định | V1 dùng **modular monolith** framework-agnostic — 1 backend process, chia module per business domain theo `DOMAIN-MAP §1, §2`. Framework cụ thể quyết Layer 3 (Sprint 0). Worker process cho async job (payout cycle, notification, reporting) **có thể** tách thành process riêng cùng codebase — quyết Layer 9 (Sprint 2). Không enforce module boundary qua tooling ở v1; discipline qua code review + DOMAIN-MAP. |
| Alternatives đã xét | (1) **Microservices** — ★ ops fit, ★★ marketplace fit do saga cho money flow + distributed tenant guard. Premature cho solo+AI MVP. (2) **Serverless / Functions** — ★★ marketplace fit do cold start ảnh hưởng booking flow + function timeout 15min không hợp payout cycle T+3 batch hours. Vendor lock. (3) **Modulith với enforced boundaries** (Nx/Turborepo/equivalent) — ★★★★ split-ready nhưng tool choice phụ thuộc framework, defer sang Layer 3. (4) **BFF + service split** — ★★★ marginal benefit, không justify cho 1 frontend chính. |
| Hệ quả tích cực | (a) 1 deploy unit, 1 log stream, 1 debug context — solo+AI friendly. (b) In-process transaction cho escrow chain (atomic, no saga overhead). (c) Single tenant filter qua guard layer cho `operatorId`. (d) AI assist quality cao (pattern phổ biến, ton of training data). (e) Time-to-v1 nhanh nhất (không trễ vì ops/infra setup). (f) Strangler-ready — split selectively post-v1 khi metric demands. |
| Hệ quả tiêu cực & mitigation | (1) **Scalability ceiling ★★★** (≈50-100k concurrent với mitigation cơ bản — đủ cho v1 và v1.x). Mitigation: horizontal scale + LB (10x), DB read replica (5-10x read), Redis cache (10-100x hot path), CDN/edge cache (-30-70% backend), worker process tách (CPU isolation). (2) **God-module risk** nếu discipline slip → mitigate qua DOMAIN-MAP enforcement trong code review. (3) **Single point of failure** → mitigate Phase 4 deploy strategy (container restart + DB replica). (4) **Big bang deploy** → mitigate Phase 4 CI/CD + feature flags. |
| Nguồn | Sprint 0 Layer 1 workshop (25/05/2026); replaces HLD-DEC-01; Khanh confirmed sau khi raise scalability ceiling concern |

### ADR-003. MongoDB là operational database chính ~~(Superseded)~~

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | **Superseded by ADR-011** (25/05/2026 — Sprint 1 Layer 4 workshop) |
| Bối cảnh (historical) | Tech stack legacy dùng NestJS + Mongoose + MongoDB. **Note 25/05/2026**: MongoDB là carryover từ codebase legacy. Full tech re-select đặt lại câu hỏi DB paradigm. OQ-14 (audit log storage) cũng re-opened do phụ thuộc DB choice. |
| Quyết định (historical) | MongoDB là database vận hành chính trong v1. |
| Kết quả supersede | ADR-011 chốt Hybrid-A: **Postgres 16 + Prisma 5** = operational DB chính; **MongoDB 7 + Mongoose** = chỉ audit/log dedicated cluster. Mongo không còn là "operational chính". |
| Nguồn | HLD §13 (Tech stack — sẽ rework Sprint 5-6); superseded bởi ADR-011 Sprint 1 Layer 4 |

### ADR-004. Redis dùng cho cache, queue và seat hold support ~~(Superseded)~~

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | **Superseded by ADR-015** (26/05/2026 — Sprint 2 Layer 8 workshop); queue specifics defer ADR-016 (Sprint 2 Layer 9) |
| Bối cảnh (historical) | Seat hold, cache search, Bull queue và retry job cần Redis hoặc lock service tương đương. **Note 25/05/2026**: Redis là carryover từ codebase legacy. Cache+lock và queue có thể tách thành 2 quyết định riêng. |
| Quyết định (historical) | Redis dùng cho cache/lock/queue support (chung). |
| Kết quả supersede | ADR-015 chốt Redis 7 (Upstash managed SG) cho **cache + lock**; ADR-016 sẽ chốt queue (Layer 9 — khả năng BullMQ trên cùng Redis instance). Vendor cụ thể (Upstash) + paradigm "1 Redis cho cả" được xác nhận, không phải "carryover" generic. |
| Nguồn | SRS DP-04, HLD; superseded bởi ADR-015 Sprint 2 Layer 8 |

### ADR-005. Payment flow dùng escrow trước payout Operator

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed |
| Bối cảnh | Managed marketplace cần bảo đảm giao dịch giữa User và Operator. |
| Quyết định | Payment thành công ghi vào escrow ledger của Platform, payout cho Operator theo T+N sau đối soát. |
| Hệ quả | Cần ledger, commission, payout, reconciliation và audit đầy đủ. |
| Nguồn | SRS MQ-01 |

### ADR-006. Payment/notification/storage dùng adapter boundary

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed |
| Bối cảnh | Provider cụ thể chưa được chốt trong SRS. |
| Quyết định | Dùng adapter boundary cho payment, email, SMS, push, object storage. |
| Hệ quả | Giảm vendor lock-in; cần contract adapter rõ ở LLD/API. |
| Nguồn | HLD-DEC-03 |

### ADR-007. Mobile dùng một codebase Expo cho User và Employee ~~(Superseded)~~

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | **Superseded by ADR-014** (25/05/2026 — Sprint 1 Layer 7 workshop) |
| Bối cảnh (historical) | SRS đã chốt mobile app dùng chung codebase cho User và Employee. **Note 25/05/2026**: Expo là carryover từ codebase legacy. Quyết định "single codebase actor-split" được closure OQ-10 ban đầu (10/05/2026). |
| Quyết định (historical) | Expo app dùng chung, tách session, navigation, UI và quyền theo actor. |
| Kết quả supersede | ADR-014 chốt **giữ Expo (managed) SDK 55+ React Native** (framework đúng) nhưng **đổi sang 2-app split**: `apps/passenger-mobile/` (public, ASO marketing) + `apps/employee-mobile/` (internal Driver + Ticket staff). Lý do tách: permission profile khác hẳn (Passenger không khai báo Background geolocation), store listing rõ ràng B2C vs B2B, bundle size nhỏ hơn. Single-codebase actor-split (closure cũ) bị thay vì làm bundle to + Apple/Google soi permission thừa. |
| Nguồn | SRS OQ-10 (closure cũ 10/05/2026, re-opened 25/05/2026); superseded bởi ADR-014 Sprint 1 Layer 7 |

### ADR-008. Admin là arbiter cuối cùng trong dispute

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed |
| Bối cảnh | Tranh chấp refund/chất lượng dịch vụ cần bên quyết định cuối cùng. |
| Quyết định | Admin Platform là arbiter cuối cùng, có thể quyết định refund đơn phương khi đủ căn cứ. |
| Hệ quả | Cần audit, reason, notification Operator, evidence workflow và permission nghiêm ngặt. |
| Nguồn | SRS MQ-03 |

### ADR-009. Backend language + runtime v1: TypeScript + Node.js LTS 22.x

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (25/05/2026 — Sprint 0 Layer 2 workshop output) |
| Bối cảnh | Layer 2 sau khi Layer 1 chốt **modular monolith framework-agnostic** (ADR-002). Cần ngôn ngữ + runtime cho 1 backend process phục vụ 3 service layer (Marketplace / Operator OS / Platform admin). Workshop 7 candidate × 8 axes (AI assist, VN vendor SDK, Khanh skill fit, concurrency model, type system, performance baseline, deploy artifact, time-to-v1). Yêu cầu: AI assist quality cao (solo+AI), VN vendor SDK fit (VNPay/eSMS/Casso), money flow correctness, time-to-v1 nhanh. Khanh self-rate: mạnh TS/JS, yếu các ngôn ngữ khác. |
| Quyết định | V1 dùng **TypeScript** (strict mode) chạy trên **Node.js LTS 22.x**. Single language full-stack: BE + FE + Mobile cùng TS, chia sẻ types qua monorepo `packages/types/`. Bun và alternative khác để mở re-evaluate post-v1 nếu metric demand. |
| Alternatives đã xét | (1) **Go** — ★★★★ performance + ★★★ AI assist + ★★ VN SDK (HTTP wrap thủ công), ★ Khanh skill. Đủ tốt nhưng learning curve cao. (2) **TS+Bun** — ★★★★ performance + ★★★★ AI assist + ★★★ ecosystem (vẫn ăn npm) nhưng runtime mới (v1.x), edge case còn nhiều, VN dev community ít — defer v1.x evaluate. (3) **Python+FastAPI** — ★★★★ AI assist nhưng type system mềm (Pydantic runtime check ≠ compile guarantee), risk cho money flow. (4) **Elixir+Phoenix** — ★★★★★ concurrency nhưng AI assist + VN SDK yếu. (5) **Rust+Axum** — ★★★★★ correctness nhưng learning curve dài, time-to-v1 chậm. (6) **Java/Kotlin (Spring/Ktor)** — JVM overhead + ecosystem nặng cho solo MVP. |
| Hệ quả tích cực | (a) Khanh skill match cao nhất (quen NestJS/Next/Expo từ legacy). (b) AI assist quality tốt nhất — Claude/Copilot mạnh nhất với TS ecosystem. (c) Type sharing BE↔FE↔Mobile qua monorepo `packages/types/` — giảm drift API contract. (d) VN vendor SDK fit tốt (VNPay, eSMS, Stringee có TS/JS SDK hoặc wrapper community). (e) Time-to-v1 nhanh nhất do không phải học ngôn ngữ mới. (f) Ecosystem npm rộng nhất: Decimal.js (money math), Zod (validation), Prisma/Drizzle/Mongoose (DB driver), Pino (log), v.v. |
| Hệ quả tiêu cực & mitigation | (1) **Performance baseline thấp hơn Go/Rust ★★** → mitigate: workload v1 <1,000 concurrent đủ headroom (xem ADR-002 mitigation); horizontal scale + LB nếu cần. (2) **Node.js single-thread CPU-bound risk** → mitigate: worker process tách cho heavy job (payout cycle, report aggregation); queue Layer 9 quyết. (3) **Money math floating-point trap** → mitigate: enforce Decimal.js hoặc BigInt cho toàn bộ money field, **không** dùng `number` raw; lint rule + code review. (4) **TypeScript chỉ là compile-time** → mitigate: strict mode + Zod schema runtime validation tại boundary (HTTP request body, queue payload, DB read). (5) **Node.js LTS lifecycle 2-2.5 năm** → mitigate: pin LTS version trong package.json + Docker image; upgrade plan trong Phase 4 deploy. |
| Nguồn | Sprint 0 Layer 2 workshop (25/05/2026); 7 candidate × 8 axes matrix; Khanh skill self-rate "Mạnh TS/JS, yếu ngôn ngữ khác" + "Đồng ý recommendation TS+Node" |

### ADR-010. Backend framework v1: NestJS 11 + nestjs-zod

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (25/05/2026 — Sprint 0 Layer 3 workshop output, Sprint 0 closing layer) |
| Bối cảnh | Layer 3 closing Sprint 0. Sau Layer 1 (modular monolith — ADR-002) + Layer 2 (TS+Node LTS — ADR-009), cần framework Node.js hỗ trợ: (a) module structure 1-1 với `DOMAIN-MAP §1, §2` enforce qua tool không chỉ convention; (b) DI built-in cho multi-tenant guard / RBAC / audit interceptor / KYC permission gate; (c) runtime validation tại boundary; (d) OpenAPI gen auto; (e) ecosystem hoàn chỉnh cho marketplace use case (queue, microservice transport — Strangler escape hatch ADR-002). Workshop 6 candidate × 9 axes (AI assist, DOMAIN-MAP fit, validation native, OpenAPI gen, DI/module, DB driver, perf, time-to-v1, Khanh skill). |
| Quyết định | V1 dùng **NestJS 11** với 2 tinh chỉnh chống "carryover trap": (1) thay `class-validator` (NestJS default) bằng **`nestjs-zod`** — Zod schema làm single source cho validate runtime + OpenAPI gen + TS type infer, end-to-end type-safe; (2) ESLint custom rule cấm import cross-module qua relative path, chỉ qua public `*.module.ts` để enforce `DOMAIN-MAP §1, §2` boundary ở mức tool (không phụ thuộc discipline solo). |
| Alternatives đã xét | (1) **Fastify 5 + Zod + awilix** — perf gấp 2-3 NestJS (~40-50k req/s), schema-first JSON Schema, OpenAPI auto. Yếu: module pattern self-roll qua plugin + DI lib ngoài → discipline cost cao cho solo+AI. (2) **Hono 4 + Zod** — perf 50-100k req/s, runtime-agnostic, type inference end-to-end. Yếu: module structure self-roll + AI assist mỏng (training data thưa) + optimize cho edge > traditional monolith. (3) **Express 5** — không module pattern + không DI native, discipline cost cao, không justify perf tradeoff. (4) **AdonisJS 6** — Laravel-clone, community VN nhỏ, AI assist yếu, learning curve Lucid ORM. (5) **Koa + tsoa** — niche, không hơn Fastify ở axis nào. |
| Hệ quả tích cực | (a) Module decorator native = `DOMAIN-MAP §1, §2` enforcement qua tool (mỗi business domain = `*Module` class). (b) DI container built-in → multi-tenant guard / audit interceptor / RBAC guard / KYC permission gate implement gọn, ít boilerplate. (c) Strangler-ready: `@nestjs/microservices` sẵn sàng extract module thành service post-v1 nếu metric demand (ADR-002 mitigation 6). (d) AI assist tốt nhất matrix — training data dồi, opinionated structure → AI gợi ý chính xác. (e) Khanh skill match 5/5 (carryover quen). (f) Ecosystem cover marketplace: `@nestjs/bull` (queue Layer 9), `@nestjs/swagger` (OpenAPI Layer 5), `@nestjs/microservices` (Strangler). (g) `nestjs-zod` thay class-validator: 1 Zod schema = validator + OpenAPI + TS type → giảm drift, type-safe end-to-end, align ADR-009 (Zod là validator standard). |
| Hệ quả tiêu cực & mitigation | (1) **Performance baseline thấp ~15-20k req/s** → mitigate: ADR-002 workload v1 <1,000 concurrent đủ headroom 50-100x; horizontal scale + LB nếu cần. (2) **Decorator boilerplate nặng** kéo dài time-to-feature-nhỏ → mitigate: `@nestjs/cli` schematic + Claude generate boilerplate. (3) **Carryover trap risk** (chọn vì quen) → mitigate: đã đánh giá 5 alternatives qua matrix 9 axes; NestJS thắng combined score, không chỉ thắng vì Khanh skill. (4) **Reflection-based DI runtime cost** → mitigate: production build dùng `swc` (NestJS support sẵn) + lazy module loading nếu cần. (5) **NestJS 11 release lifecycle** → mitigate: pin version trong package.json + Docker image; upgrade plan Phase 4. (6) **`nestjs-zod` library nhỏ hơn class-validator** (community size) → mitigate: nếu fail, fallback class-validator + `@anatine/zod-openapi` (Zod → OpenAPI standalone) → vẫn giữ Zod single source. |
| Nguồn | Sprint 0 Layer 3 workshop (25/05/2026); 6 candidate × 9 axes matrix; Khanh chốt "NestJS 11 + nestjs-zod (Recommended)" |

### ADR-011. DB paradigm v1: Hybrid-A (Postgres 16 ops + Mongo 7 audit) + Prisma 5 / Mongoose

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (25/05/2026 — Sprint 1 Layer 4 workshop output) |
| Bối cảnh | Layer 4 mở đầu Sprint 1 Phase 1 Foundation. Sau ADR-002/009/010 chốt modular monolith + TS+Node+NestJS, cần DB phục vụ: (a) money flow (escrow ledger, commission, payout, refund) — ACID strict; (b) multi-tenant qua `operatorId` (DM-01, FR-OPR-11, FR-IAM-06); (c) audit log (OQ-14 re-opened) — append-only, volume ~30k-90k entries/tháng v1; (d) reporting (OQ-15) cho operator + admin dashboard; (e) operational core (Booking/Payment/Trip/Vehicle/Route/User/Operator/KYC/Dispute/Fare). Workshop 5 paradigm × 10 axes. Khanh self-rate "Mạnh cả SQL + NoSQL" → skill không phải barrier cho 2-paradigm. |
| Quyết định | V1 dùng **Hybrid-A**: <br>• **Operational DB = PostgreSQL 16** (managed via Supabase / Neon / RDS SG region) — handle **toàn bộ entity operational** (User, Operator, Employee, KYC, Trip, Route, Vehicle, Seat, Booking, Payment, Escrow, Commission, Payout, Refund, Dispute, Fare, Notification record). ORM = **Prisma 5** + `@nestjs/prisma` style integration. <br>• **Audit DB = MongoDB 7** (cluster RIÊNG, Atlas SG hoặc self-host) — chỉ chứa `audit_event` + `system_log` (time-series collection). ORM = **Mongoose 8** + `@nestjs/mongoose`. <br>• **Money type**: VND amount column = `BIGINT` (đơn vị đồng, integer-only); commission % calc trong app dùng `Decimal.js` theo ADR-009 mitigation 3. <br>• **Multi-tenant**: app-layer `operatorId` filter (NestJS guard) + Postgres **Row-Level Security (RLS)** policy defense-in-depth. <br>• **Audit append-only**: REVOKE UPDATE/DELETE permission cho app DB user trên Mongo `audit_event` collection; chỉ admin DBA + migration job có quyền (Phase 4 deploy). |
| Alternatives đã xét | (1) **Postgres-only (Relational)** — ★★★★★ ACID + RLS + JSONB. Yếu: audit volume tăng lâu dài có thể stretch single cluster, không có dedicated audit-optimized engine. (2) **Mongo-only (Document NoSQL)** — ★★★ ACID multi-doc txn limit; escrow chain stretch sweet spot; AI assist cho aggregation kém SQL CTE; drift schema risk solo+AI. (3) **MySQL 8** — không RLS native; JSON column yếu hơn JSONB; CTE/partition kém Postgres. Không có lý do chọn khi không legacy constraint. (4) **NewSQL CockroachDB** — overkill v1, $300+/mo, VN region chưa có. Defer post-v1 nếu sharding cần. (5) **Polyglot (Postgres + ClickHouse)** — 3+ service quá nặng solo+AI v1. Defer post-v1 nếu analytics volume đòi. (6) **Hybrid-B/C** (Mongo footprint rộng hơn) — không justify khi audit-only đủ; broader Mongo = thêm ORM coverage không cần thiết. |
| Hệ quả tích cực | (a) **Money correctness** — Postgres ACID + savepoint cho escrow chain atomic (Booking + Payment + EscrowEntry + CommissionEntry trong 1 txn, no saga). (b) **Multi-tenant defense-in-depth** — RLS Postgres = layer 2 sau app guard; lỡ app miss filter vẫn an toàn (relevant cho SEC §6 rework Sprint 5-6). (c) **AI assist top tier** — Prisma + SQL training data dồi nhất; AI gen migration + query chính xác. (d) **JSONB escape hatch** — flexible field (seat layout config, KYC metadata, dispute evidence list) vẫn ở Postgres ACID, không cần Mongo cho "flexible schema" excuse. (e) **Audit isolation** — Mongo dedicated cluster, time-series collection optimized cho append-only + retention; backup/restore độc lập operational; volume scaling độc lập post-v1. (f) **Khanh skill match** ("Mạnh cả SQL + NoSQL") — 2-paradigm không là barrier. (g) **Reporting**: Postgres CTE + materialized view + async refresh job (Layer 9 queue) cho structured business report; Mongo aggregation cho audit-style query nội bộ — re-close OQ-15 implementation. (h) **OQ-14 re-closed** với wording cập nhật: Mongo cluster RIÊNG (không phải "same cluster" như closure cũ 11/05/2026), append-only via permission. (i) **Cost VN region**: Supabase $25/mo + Atlas M10 SG $60/mo ≈ $85/mo, vẫn rẻ hơn polyglot. |
| Hệ quả tiêu cực & mitigation | (1) **2x ops cost** (backup, monitor, version upgrade, replication) → mitigate: Khanh confirm aware via "Mạnh cả SQL + NoSQL" self-rate; managed services (Supabase + Atlas) tự handle ops phần lớn. (2) **2 ORM trong cùng monolith** (Prisma + Mongoose) → mitigate: tách module rõ — `audit/` module dùng Mongoose, các module operational chỉ dùng Prisma; ESLint rule cấm cross-import driver. (3) **Cross-DB join không có** — audit query muốn enrich từ operational data phải fetch riêng → mitigate: snapshot reference data (vd `actorRole`, `targetEntityType`, `operatorId`) vào audit event tại write time, không lazy lookup. (4) **Money math floating-point trap** → đã mitigate ADR-009 (Decimal.js + BigInt + lint rule); column type Postgres = `BIGINT` cho VND amount. (5) **Schema migration discipline 2 DB** → mitigate: Prisma Migrate cho Postgres; Mongo schema-less không cần migration nhưng add Mongoose schema strict + version field cho audit_event. (6) **Reporting cross-DB** — nếu cần join audit với operational → option A: async ETL từ audit Mongo → Postgres materialized view; option B: defer cho post-v1 ClickHouse OLAP. Quyết định cụ thể khi rework SDLC doc 04-DB Sprint 5-6. |
| Nguồn | Sprint 1 Layer 4 workshop (25/05/2026); 5 paradigm × 10 axes matrix; Khanh self-rate "Mạnh cả SQL + NoSQL" + chốt Hybrid-A + Prisma 5. Supersedes ADR-003. Re-closes OQ-14, refines OQ-15 implementation. |

### ADR-012. API style v1: REST + OpenAPI 3.1 auto (Zod single source)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (25/05/2026 — Sprint 1 Layer 5 workshop output) |
| Bối cảnh | Layer 5 sau Layer 1-4 (modular monolith + TS+Node+NestJS+nestjs-zod + Hybrid-A DB). Cần API style cho 3 client TS (Web Layer 6, Mobile Layer 7, Admin Layer 6) + 3rd party Operator integration (push trip data, sync schedule) + payment gateway webhook inbound (callback Sprint 3 vendor). Constraint quan trọng: **Zod schema là single source** (nestjs-zod chốt Layer 3) → pipeline tự nhiên Zod → OpenAPI → client gen. Workshop 5 candidate × 10 axes (type safety E2E, AI assist, OpenAPI export, caching, mobile fit, schema evolution, NestJS fit, client tooling, learning curve, 3rd party fit). |
| Quyết định | V1 dùng **REST + OpenAPI 3.1** với toolchain: <br>• **Server**: NestJS 11 + nestjs-zod + `@nestjs/swagger` → auto-gen OpenAPI 3.1 spec từ Zod schema. <br>• **Client gen**: `openapi-typescript` (gen TS types từ OpenAPI YAML) + chốt 1 trong `orval` hoặc `@hey-api/openapi-ts` (gen client + React Query hook) — tool cụ thể quyết ở Sprint 5-6 rework SDLC 05-API. <br>• **Versioning**: URL prefix `/v1/*`; breaking change → `/v2/*` mới; field deprecation qua OpenAPI `deprecated: true` + response header `Deprecation: <date>`. <br>• **Webhook inbound** (payment callback, KYC verification result): REST POST + HMAC-SHA256 signature verification + timestamp + nonce; adapter boundary (ADR-006) per provider. <br>• **Error format**: RFC 7807 Problem Details (`application/problem+json`) với `type`, `title`, `status`, `detail`, `instance`, `code` (GLOSSARY error code). |
| Alternatives đã xét | (1) **tRPC** — type infer instant nhưng NestJS không native (`trpc-nest` community) + 3rd party Operator non-TS không xài + OpenAPI partial qua plugin. Defer. (2) **GraphQL** — mobile over-fetch risk, HTTP cache khó (POST + body query → CDN custom), Operator phải learn GraphQL barrier. Overkill v1. (3) **gRPC + Protobuf** — không fit client-facing browser/mobile (grpc-web proxy thêm complexity); defer cho Strangler service-to-service post-v1. (4) **Hybrid REST public + tRPC internal** — best capability nhưng 2 paradigm setup không justify v1; defer post-v1 nếu type-share friction đo được. |
| Hệ quả tích cực | (a) **Zod schema = single source** end-to-end: từ Layer 3 nestjs-zod → OpenAPI 3.1 auto → client gen → API docs (Scalar/Redoc) → mock server (Prism) → contract test (Pact/Dredd). 1 nguồn, 0 drift. (b) **AI assist top tier** — REST + OpenAPI là pattern phổ biến nhất, AI gen endpoint/DTO/test/client chính xác. (c) **3rd party fit ★★★★★** — Operator + payment gateway dùng REST + OpenAPI là industry standard; SDK gen từ OpenAPI YAML ngôn ngữ nào (Java/Python/PHP/Go) cũng được. (d) **HTTP semantics + caching** — ETag/Cache-Control/CDN cho GET endpoint free 30-70% backend load khi scale (align ADR-002 mitigation). (e) **Mobile fit ★★★★★** — JSON + URL semantics, offline cache layer dễ implement. (f) **Khanh skill match 5/5** — REST quen từ NestJS legacy. (g) **Versioning rõ ràng** — URL prefix dễ communicate cho Operator + dev. (h) **Type safety E2E qua build step** — `openapi-typescript` gen TS types từ OpenAPI YAML; monorepo CI auto-gen `packages/api-client/` khi schema thay đổi; lock file fail-fast nếu out-of-sync. (i) **Error format chuẩn** RFC 7807 → consistent error handling cho cả 3 client + 3rd party. |
| Hệ quả tiêu cực & mitigation | (1) **Type infer không "instant" như tRPC** (cần codegen step) → mitigate: monorepo CI auto-gen `packages/api-client/` khi schema thay đổi; lock file để CI fail nếu out-of-sync; pre-commit hook check. (2) **Boilerplate cho mỗi endpoint** (Zod schema + controller + service) → mitigate: nestjs-zod giảm 50% boilerplate vs class-validator; NestJS CLI schematic + Claude gen template. (3) **HTTP verb mapping cho complex action** ("approve KYC", "release escrow", "trigger payout") → mitigate: POST + sub-resource URL convention (`POST /admin/kyc/{id}/approve`, `POST /admin/escrow/{id}/release`, `POST /admin/payout/{id}/confirm`); document convention trong 05-API rework Sprint 5-6. (4) **OpenAPI 3.1 vs 3.0 ecosystem gap** — một số tool cũ chỉ support 3.0; nestjs-zod hiện gen 3.0 → mitigate: sticky 3.0 v1; upgrade 3.1 khi tool ecosystem catches up (`openapi-typescript` v7+ đã support 3.1). (5) **Webhook security** — HMAC verification cần per-provider implementation → mitigate: adapter boundary (ADR-006) cho mỗi provider; standard HMAC-SHA256 + timestamp + nonce; idempotency key cho retry. (6) **Codegen drift risk** giữa BE schema và FE generated client → mitigate: CI step `pnpm gen:api-client` chạy sau migration; commit lock file; PR check fail nếu schema và client mismatch. |
| Nguồn | Sprint 1 Layer 5 workshop (25/05/2026); 5 candidate × 10 axes matrix; Khanh chốt "REST + OpenAPI auto (Recommended)". Unblock SDLC doc 05-API rework Sprint 5-6. |

### ADR-013. Frontend framework v1: Next.js 16 App Router + Turborepo monorepo

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (25/05/2026 — Sprint 1 Layer 6 workshop output) |
| Bối cảnh | Layer 6 sau Layer 1-5 (modular monolith + TS+Node+NestJS+nestjs-zod + Hybrid-A DB + REST/OpenAPI). Cần FE framework cover 3 web surface: **Marketplace** (passenger public-facing, **SEO matter** — landing + search trip), **Operator OS** (internal dashboard sau auth), **Admin** (internal dashboard sau auth). Plus: monorepo structure để share Zod schemas (Layer 3) + API client gen (Layer 5) + UI lib. Workshop 6 candidate × 11 axes (SEO/SSR, TS first-class, AI assist, ecosystem, DX/build, deploy fit, mobile UI share, multi-app monorepo, Khanh skill, bundle, maintenance) + monorepo vs multi-repo comparison 13 axes. |
| Quyết định | V1 dùng **Next.js 16 App Router** trong **Turborepo + pnpm workspace monorepo**. <br><br>**Folder structure**: <br>`apps/api/` (NestJS 11 BE — modular monolith) <br>`apps/marketplace/` (Next.js — passenger public, RSC + SSG/ISR cho SEO) <br>`apps/operator-os/` (Next.js — operator + employee dashboard, CSR + middleware auth) <br>`apps/admin/` (Next.js — platform admin dashboard, CSR + middleware auth) <br>`packages/types/` (Zod schemas — single source từ Layer 3) <br>`packages/api-client/` (gen từ OpenAPI Layer 5, build artifact committed) <br>`packages/ui/` (shared component lib — tool cụ thể chốt Sprint 5-6) <br>`packages/utils/` (Decimal.js wrappers, formatters, validators) <br>`packages/config/` (ESLint, TSConfig base, Tailwind preset). <br><br>**App count cụ thể** (3 app riêng vs 2 app gộp `back-office/` với role-switch) **defer LLD Sprint 5-6** — không ảnh hưởng Layer 6 ADR. <br>**Component library** defer Sprint 5-6 — recommend **Shadcn/ui + Tailwind CSS 4** (alternatives: Mantine 8 / Ant Design 5 / Chakra UI 3). |
| Alternatives đã xét | **Framework**: (1) **Remix v3** — web standards Fetch API + form-first action, no RSC overhead. Khanh chưa quen learning curve 1-2 tuần; ecosystem nhỏ hơn Next. (2) **Vite + React Router 7** — SPA default, không SSR mặc định → Marketplace SEO yếu; force split 2-stack với Next cho marketplace không justify. (3) **SvelteKit 2** — bundle nhỏ DX cao; Khanh skill barrier (React→Svelte switch); mobile Layer 7 RN không share component. (4) **Astro 5 + React island** — content-first không fit dashboard rich-interaction; hybrid stack complexity. (5) **TanStack Start** — promising type-safe routing nhưng production-ready chưa firmly, AI assist yếu. Defer re-evaluate post-v1. <br><br>**Repo structure**: **Multi-repo (3 Git repo)** — permission boundary tốt hơn nhưng solo+AI không relevant; share Zod schemas + API client qua npm publish painful cho v1 atomic change; cross-app navigation kém cho AI assist. |
| Hệ quả tích cực | (a) **3 surface cover trong 1 framework + 1 monorepo** — Marketplace SEO (RSC + SSG + ISR), Operator OS + Admin (CSR + middleware auth). 1 tooling, 1 CI, 1 deploy pipeline. (b) **Shared Zod schemas → API client native** qua workspace package — không cần npm publish, atomic cross-app change. (c) **Solo+AI velocity tối ưu** — Claude/Copilot load full context cross-app, suggest sync BE+FE changes; Turborepo cache speed up build. (d) **AI assist top tier** — Next.js App Router pattern phổ biến nhất 2024-2026, AI gen RSC/loader/middleware chính xác. (e) **Khanh skill match 5/5** carryover Next; matrix verify Next thắng combined score, không chỉ familiarity bonus. (f) **Strangler-ready** — sau v1 nếu cần tách `apps/marketplace/` ra repo riêng + publish npm: 1-2 ngày work. (g) **Performance** — RSC giảm client JS đáng kể; Next Image/Font/Script optim built-in; ISR cache cho Marketplace public pages (align ADR-002 mitigation CDN/edge cache 30-70%). (h) **Deploy fit** — Vercel/Netlify 1-click hoặc standalone Docker (giữ option Layer 11 deploy target Sprint 4). (i) **Turborepo task graph + remote cache** (Vercel Remote Cache free hoặc self-host) → build incremental, only affected apps rebuild. |
| Hệ quả tiêu cực & mitigation | (1) **RSC vs Client component boundary** mental overhead → mitigate: ESLint rule + convention `"use client"` directive top-level; AI assist gen pattern đúng; LLD Sprint 5-6 document convention. (2) **Bundle nặng** so với SvelteKit/Vite → mitigate: RSC giảm client JS đáng kể; route-level code split native; Next optim; ISR cache. (3) **Carryover trap risk** → mitigate: matrix evaluate 5 alternatives, Next thắng combined score + Khanh skill bonus, không familiarity alone. (4) **Mobile UI share kém native** với Layer 7 — RSC không chạy trên React Native → mitigate: Layer 7 workshop tiếp theo sẽ quyết framework Mobile riêng; share qua `packages/types` + `packages/api-client` + design tokens; không strict share component Web RSC ↔ Mobile RN. (5) **Monorepo code review surface lớn** khi PR cross-app → mitigate: solo+AI v1 chính Khanh review không real concern; CODEOWNERS + PR template khi team scale post-v1. (6) **Build time tăng theo size monorepo** → mitigate: Turborepo remote cache + CI affected detection (`turbo run build --filter=...`). (7) **Permission boundary mở** monorepo → mitigate: solo dev không relevant; team scale dùng GitHub branch protection + CODEOWNERS. |
| Nguồn | Sprint 1 Layer 6 workshop (25/05/2026); 6 framework × 11 axes matrix + monorepo vs multi-repo comparison 13 axes; Khanh chốt "Next.js 16 App Router (Recommended)" + "Monorepo (Turborepo + pnpm)". App count detail + component lib defer LLD Sprint 5-6. |

### ADR-014. Mobile framework v1: Expo (managed) SDK 55+ React Native, 2 app split (Passenger + Employee)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (25/05/2026 — Sprint 1 Layer 7 workshop output, Sprint 1 closing layer) |
| Bối cảnh | Layer 7 closing Sprint 1. Sau Layer 1-6 (modular monolith + TS+Node+NestJS+nestjs-zod + Hybrid-A DB + REST/OpenAPI + Next.js+Turborepo monorepo), cần framework Mobile cover **3 actor mobile-grade**: **Passenger** (tìm/đặt vé, e-ticket QR, push, payment redirect deep link), **Driver** (manifest, **background geolocation** nếu có live tracking, push, scan QR khách lên xe), **Ticket staff** (scan QR **primary** tại bến, manual override, share intent printer). Operator admin + Platform support dùng Web (Layer 6), không cần mobile. OQ-10 re-opened (closure cũ 10/05/2026 = "single Expo codebase actor-split"). Workshop 6 candidate × 14 axes (AI assist, Khanh skill fit, code share Next, type/schema share BE, Camera, Background geo, Push FCM+APNs, Biometric, Store submit, OTA, Perf, Time-to-v1, Ecosystem VN, Strangler). |
| Quyết định | V1 dùng **Expo (managed workflow) SDK 55+** React Native + TypeScript, New Architecture (Fabric) enabled. <br><br>**App architecture: 2 app split** trong cùng monorepo Turborepo (Layer 6): <br>• `apps/passenger-mobile/` — Passenger-facing, public store listing "Vé Xe Nhanh", ASO marketing optimization, permission minimal (Camera + Push + Biometric optional + Deep link). <br>• `apps/employee-mobile/` — Internal Operator employee (Driver + Ticket staff), store listing "Vé Xe Nhanh Operator", permission full (Camera + **Background geolocation** + Push + Biometric + Share intent). <br>• Shared packages: `packages/types/` (Zod schemas Layer 3), `packages/api-client/` (gen từ OpenAPI Layer 5), `packages/ui-mobile-shared/` (RN components dùng chung — design tokens + primitives), `packages/utils/` (Decimal.js wrappers, formatters). <br><br>**Build/Release**: **EAS Build cloud** (Windows dev OK, không cần Mac local) + **EAS Update** (OTA push bug fix không qua store review) + **EAS Submit** (auto submit Apple + Google). 2 EAS project song song. <br><br>**Push**: FCM (Android) + APNs (iOS) qua Expo Push API hoặc raw `react-native-firebase` — quyết cụ thể ở Phase 3 Layer 13 (Push provider Sprint 3). <br><br>**Native API trong Expo SDK** (không eject v1): `expo-camera` (QR scan), `expo-barcode-scanner`, `expo-task-manager` + `expo-location` background (Driver), `expo-notifications` (push), `expo-local-authentication` (biometric), `expo-secure-store` (Keychain/Keystore token storage). |
| Alternatives đã xét | (1) **RN bare workflow** — full native control, eject từ đầu. Yếu: mất EAS Update (CodePush sunset 2024-2025), Windows dev iOS phải qua CI/Mac cloud, time-to-v1 +1 sprint vs Expo managed. Strangler path Expo → RN bare là official, defer khi cần native module ngoài Expo SDK. (2) **Flutter 3** — AOT compile Dart, UI 60fps mượt. Yếu: Khanh phải học Dart (ngôn ngữ thứ 3 sau TS), không share types với BE/Web (orval/openapi-typescript không gen Dart, phải dùng openapi-generator riêng), AI assist Dart < RN, ecosystem VN nhỏ hơn. (3) **Capacitor + Web (Ionic)** — wrap Next.js trong native shell, share ~90% code Web. Yếu: cold start webview chậm, **Background geolocation kém** (Driver gặp khó), Camera scan QR liên tục trên WKWebView UX kém native. (4) **PWA only** — không native app, chỉ Service Worker + Web Push API. Yếu: ✕ Background geolocation (browser cấm), ✕ iOS Safari 16.4+ Web Push giới hạn, payment redirect deep link kém, Camera UX iOS Safari hạn chế nhiều. Loại nguyên cứu cho Passenger-only. (5) **Native Swift + Kotlin** — 2 codebase, hiệu năng tối đa. Yếu: ✕ Khanh skill, ✕ time-to-v1 (5+ sprint cho 2 app × 2 native = 4 codebase), không justify v1 solo+AI. <br><br>**App architecture alternatives**: (a) **1 app actor-split UI** (carryover OQ-10 cũ) — bundle to (ship cả Passenger + Employee UI), permission Background geo phải khai báo cho mọi user (Apple/Google soi vì permission thừa), store listing khó định vị B2C vs B2B. (b) **3 app split** (Passenger / Driver / Staff riêng) — permission siêu-minimal nhưng ops 3x (3 EAS project, 3 store listing) — quá fragment cho v1. (c) **Passenger PWA + Employee native** — tiết kiệm 1 store submit nhưng mất ASO Passenger + iOS Web Push giới hạn. |
| Hệ quả tích cực | (a) **Khanh skill match 5/5** — RN = React + JSX + TS, reuse 90% mental model từ Layer 6 Next. (b) **EAS Build cloud** giải quyết Windows dev (không cần Mac local) + auto sign + store submit + OTA update — single biggest unblocker cho solo+AI Mobile. (c) **AI assist top tier** — RN + Expo training data dồi, Claude/Copilot gen screen/hook/native module chính xác; Dart/Swift/Kotlin training thưa hơn nhiều. (d) **Type/schema share BE** qua `packages/types/` Zod — single source contract giữa NestJS BE và 2 mobile app; API client tự gen từ OpenAPI Layer 5 → `packages/api-client/` shared cả Web + Mobile. (e) **Mọi native API cần thiết trong Expo SDK** (Camera, Background geo, Push, Biometric, Secure store, Share intent) — **không cần eject v1**. (f) **EAS Update OTA** — ship bug fix mobile không qua Apple/Google review (1-7 ngày), critical cho v1 fast iteration. (g) **Time-to-v1 ngắn nhất** matrix (2 sprint cho 2 app gộp shared package, vs 3+ cho RN bare/Flutter). (h) **2-app split tốt hơn 1-app**: permission minimal đúng vai trò (Passenger không khai báo Background geo → Apple/Google không soi), bundle nhỏ → cold start nhanh, store listing rõ ràng B2C "Vé Xe Nhanh" vs B2B "Vé Xe Nhanh Operator", ASO marketing tách cho Passenger. (i) **Strangler-ready** — eject sang RN bare là path Expo official; nếu sau v1 cần native module đặc thù (vd printer SDK độc quyền cho Ticket staff hoặc map provider native), 1-2 ngày work eject 1 trong 2 app, app còn lại giữ managed. (j) **Ecosystem VN** — RN cộng đồng VN lớn nhất trong các mobile cross-platform; nhiều startup VN dùng Expo (Tiki, MoMo, ShopeeFood đều có RN screen). |
| Hệ quả tiêu cực & mitigation | (1) **Performance JS bridge baseline thấp hơn native/Flutter** (cold start 1-2s, animation 60fps cần Fabric/Reanimated) → mitigate: New Architecture (Fabric) enable từ đầu; marketplace booking flow không heavy animation/3D, không phải bottleneck v1; perf optim defer khi metric demand. (2) **2 app permission profile khác nhau** kéo theo 2 `app.json` config (Expo plugin) + 2 set test → mitigate: shared `packages/ui-mobile-shared/` cover logic chung; 2 `app.config.ts` riêng cho permission/plugin/native config; CI matrix build 2 app parallel. (3) **ASO marketing tách 2 store listing** (Passenger public, Employee internal) → mitigate: Employee app set "Unlisted" hoặc invite-only TestFlight/Internal Testing → giảm friction; Passenger app dùng growth/marketing slot chính. (4) **EAS Build 2 project ops** (cost EAS Production tier có cap monthly build) → mitigate: EAS Production tier $99/mo cover 100 build/mo đủ cho 2 app v1; remote cache + only-affected build qua Turborepo `--filter`. (5) **Deep link payment redirect** từ VNPay sandbox → mobile app (callback URL) cần URL scheme/Universal Link config riêng mỗi app → mitigate: Passenger app có URL scheme `vexenhanh://`, Employee app `vexenhanh-operator://`; document trong 05-API rework Sprint 5-6 spec callback per provider. (6) **Push token per app** (2 push token cho 1 user nếu user dùng cả 2 — không khả thi, vai trò khác nhau) → không thực tế concern vì 1 user 1 vai trò. Trường hợp Operator admin muốn nhận push notification cả Passenger app + Employee app, mitigate qua Web push (Layer 6 Operator OS) thay vì cài cả 2 app. (7) **Eject path nếu cần native module ngoài Expo SDK** → mitigate: theo dõi monthly Expo SDK release; nếu cần module v1 chưa support (chưa thấy nhu cầu), eject app cần ấy trước, app còn lại giữ managed; cost ~1-2 ngày work. |
| Nguồn | Sprint 1 Layer 7 workshop (25/05/2026); 6 candidate × 14 axes matrix + app architecture sub-decision 4 option; Khanh chốt "Expo (managed) SDK 55+ (Recommended)" + "2 app — Passenger + Employee tách (Recommended)". Supersedes ADR-007. Re-closes OQ-10. Sprint 1 closed (4/4 Phase 1 layer hoàn tất). |

### ADR-015. Cache + Distributed Lock v1: Redis 7 (Upstash managed SG)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (26/05/2026 — Sprint 2 Layer 8 workshop output) |
| Bối cảnh | Layer 8 mở đầu Sprint 2 Phase 2 Infrastructure. Sau Layer 1-7 (modular monolith + TS+Node+NestJS+nestjs-zod + Hybrid-A DB + REST/OpenAPI + Next.js+Turborepo + Expo 2-app), cần hạ tầng cho **2 chức năng critical**: (1) **Distributed lock** cho seat hold 10 phút (FR-BKG, OQ-06), idempotency payment callback, payout cycle batch — nếu thiếu → overbooking + double-charge + double-pay; (2) **Cache** cho trip search (giảm DB load 50-95% hot path), session lookup, operator info — nếu thiếu → DB nóng + latency tăng. Khanh self-rate "nghiệp dư từ Layer 8" — ưu tiên simple + AI-assist-friendly + low ops learning curve. Workshop 4 paradigm × 12 axes + concept primer chi tiết (cache là gì, lock là gì, use case marketplace, decision tree 1-instance vs distributed). |
| Quyết định | V1 dùng **Redis 7** trên **Upstash managed Singapore** cho cả cache + distributed lock + (nhiều khả năng) Layer 9 queue.<br><br>**Vendor**: **Upstash Redis managed** region `ap-southeast-1` (Singapore) — serverless pay-per-request, không phải vận hành ops (backup, replication, failover).<br><br>**Stack TS/NestJS**:<br>• Driver: `ioredis` (standard Redis protocol, KHÔNG dùng Upstash REST API → migrate ra Redis nào cũng dễ).<br>• Cache: `@nestjs/cache-manager` + `cache-manager-redis-yet`.<br>• Lock: custom NestJS service wrap `SET key value NX EX <ttl>` + `EVAL`-based release script (Redlock-style single-node).<br>• Type-safe key pattern: enum + builder helper trong `packages/utils/`.<br><br>**Seat hold implementation v1**: **Pure Redis** `SET seat:{tripId}:{seatId}:hold {bookingId} NX EX 600`. Atomic guarantee qua optimistic concurrency control — booking create verify seat hold key vẫn tồn tại + owned by self trước khi commit. **Hybrid** (Redis lock + Postgres `seat_hold` table atomic với booking transaction) defer LLD Sprint 5-6 — chỉ refactor nếu race condition production đo được.<br><br>**Cache invalidation strategy**: **TTL-based default** (60s search, 1h operator info, 24h seat layout, 15min session) — không invalidate chủ động trừ khi mutation rõ ràng (Operator update → `DEL operator:{id}`). KISS, tránh stale read horror.<br><br>**Fallback policy**: Redis unreachable → NestJS HealthCheck interceptor → booking endpoint trả **503 Service Unavailable** (user retry 30s). **KHÔNG fallback in-memory lock** (sẽ overbooking nếu 2 instance). Cache miss khi Redis down → fall through query DB (acceptable latency degradation). |
| Alternatives đã xét | (1) **Paradigm B — Pure Postgres** (DIY `seat_hold` table + unique constraint + cron clean expired + `pg_advisory_lock` cho batch; LRU in-process cache; pg-boss queue). 0 infra mới nhưng pattern không phổ biến → AI assist yếu + edge case nhiều (clock drift, missed cron, holder crash). Không phù hợp Khanh nghiệp dư. (2) **Paradigm C — Hybrid** (Postgres lock cho seat/payment atomic + Redis cho cache + session). Best-of-both nhưng 2 paradigm cùng lúc → gấp đôi learning cost. Phù hợp senior team. (3) **Paradigm D — Defer** (v1 = 1 instance + in-memory mutex + LRU cache, không distributed). 0 infra mới nhưng deploy = downtime 10-30s + cửa sổ overbooking; migrate distributed sau v1 tốn 1-2 sprint audit critical section. (4) **Vendor alternatives** trong Paradigm A: **Redis Cloud** (Redis Inc official) — UI tốt, enterprise feature thừa v1; **Self-host VPS** ($5/mo) — Khanh phải biết Redis ops (AOF, RAM monitor, restart) không phù hợp nghiệp dư; **DragonflyDB** — drop-in 25x throughput nhưng mới (2022), AI assist mỏng, defer post-v1; **Memcached** — không lock primitives + không persistence → seat hold mất khi restart, loại. |
| Hệ quả tích cực | (a) **Khanh chỉ học 1 concept mới** — Redis basic (`GET/SET/DEL/SET NX EX/EXPIRE`) 1 buổi đủ cho v1. (b) **AI assist top tier** — Redis là pattern phổ biến nhất ở backend; Claude/Copilot/Stack Overflow đều có context chính xác khi Khanh stuck. (c) **Layer 9 (queue) tự động được hưởng lợi** — BullMQ trên cùng Redis Upstash là default path, không cần thêm vendor. (d) **Upstash managed = 0 ops Khanh** — backup, replication, failover, monitoring đều Upstash lo; Khanh chỉ thấy URL connection string. (e) **Cost thấp v1** — Upstash free tier 10k req/day; Pro $0.2/100k req. Marketplace volume 1k user × 100 req/day × Redis 30% hit ≈ ~$10/mo. (f) **NestJS ecosystem mạnh nhất** — `@nestjs/cache-manager`, `@nestjs/bull`, `ioredis`, RedisModule chính chủ. (g) **Strangler-ready** — `ioredis` standard protocol → switch DragonflyDB / Redis Cluster / Redis self-host bằng 1 dòng đổi URL khi cần. (h) **Region SG ~30-50ms từ VN** — acceptable cho cache (vs DB query 5-50ms từ Postgres SG cũng same range). (i) **Pure Redis seat hold đơn giản nhất** — `SET NX EX 600` semantic atomic + auto-release, không DIY cron clean. (j) **TTL-based cache invalidation tránh "2 hard problems"** — stale data tự expire, không phải build invalidation graph phức tạp. |
| Hệ quả tiêu cực & mitigation | (1) **Redis down → booking flow fail** (Upstash uptime 99.95% → ~4 giờ/năm downtime worst case) → mitigate: NestJS HealthCheck interceptor; booking endpoint trả 503 cho user retry 30s — **KHÔNG fallback in-memory lock** (overbooking risk); cache miss → fall through DB (graceful degradation latency). (2) **Vendor lock Upstash** → mitigate: chỉ dùng `ioredis` standard Redis protocol, **KHÔNG dùng Upstash REST API** (specific Upstash); migrate sang Redis self-host / DragonflyDB / Redis Cluster bất kỳ lúc nào bằng đổi `REDIS_URL` env var. (3) **Cache invalidation phức tạp khi data mutation rộng** (vd update Operator name → invalidate trip search cache có chứa operator info) → mitigate: TTL ngắn (60s search) giảm window stale; chỉ invalidate chủ động khi mutation owner entity (Operator update → `DEL operator:{id}`); document convention trong LLD Sprint 5-6. (4) **Cost monitoring** Upstash pay-per-request → mitigate: setup alert khi req/day > 50k baseline; cache key design tránh hot-key (vd không cache per-user per-request key, dùng per-route per-date key); review monthly. (5) **Ops learning curve** Khanh — mitigate: Khanh chỉ cần biết 5 command (`GET/SET/DEL/SET NX EX/EXPIRE`); Upstash UI dashboard cho mọi observability; AI assist + Stack Overflow cover khi stuck. (6) **Seat hold race condition pure Redis** (booking create giữa lúc seat hold gần expire) → mitigate: booking create verify seat hold key tồn tại + ownership trong cùng Lua script `EVAL` atomic; nếu production race đo được, refactor sang Hybrid (Postgres `seat_hold` table) trong LLD Sprint 5-6 — không premature. |
| Nguồn | Sprint 2 Layer 8 workshop (26/05/2026); 4 paradigm × 12 axes matrix + concept primer cho Khanh nghiệp dư; Khanh chốt "A. Redis cho cả cache + lock (Recommended)" + "Upstash managed SG (Recommended)". Supersedes ADR-004. |

### ADR-016. Async Job Queue v1: BullMQ + @nestjs/bullmq trên Redis Upstash

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (26/05/2026 — Sprint 2 Layer 9 workshop output) |
| Bối cảnh | Layer 9 sau ADR-015 Layer 8 (Redis Upstash). Marketplace có ~10 use case async: booking confirm fan-out (SMS/email/push), audit log → Mongo batch write, payout cycle T+3 cron, webhook delivery → Operator (exponential retry + DLQ), refund process async, KYC document verify (manual chính v1, OCR roadmap post-v1), reporting aggregation hourly/daily, push notification batch, expired booking cleanup, OTP rate limit cleanup. Critical use case: payout T+3 (tiền), booking confirm UX, webhook (integration). Khanh self-rate "nghiệp dư từ Layer 8" → ưu tiên AI-assist-friendly + NestJS native + 0 infra mới. Workshop 4 candidate × 12 axes + concept primer (sync vs async, producer/queue/worker/retry/DLQ/scheduler/workflow) + 10 use case mapping. |
| Quyết định | V1 dùng **BullMQ** + **`@nestjs/bullmq`** chạy trên **cùng Redis Upstash SG** (Layer 8). <br><br>**Stack TS/NestJS**: <br>• `bullmq` (npm) — Redis-based queue, TS-native, MIT license <br>• `@nestjs/bullmq` — official NestJS integration, decorator `@Processor('queue-name')` + `@Process('job-name')` <br>• Dashboard: `@bull-board/express` + `@bull-board/api` — open-source UI mount tại `/admin/queues` (NestJS guard cho admin role) <br>• `ioredis` reuse từ ADR-015 (cùng connection pool) <br><br>**Job pattern v1**: <br>• Fire-and-forget fan-out (booking confirm SMS/email/audit/push): retry 3× exponential 30s/5min/1h <br>• Scheduled cron (payout T+3): `repeat: { pattern: '0 0 * * *' }` + BullMQ rate limiter lock single worker <br>• Delayed retry với DLQ (webhook Operator): exponential 30s/5min/1h/6h/24h/7d, sau 7 retry → DLQ manual review <br>• Multi-step chain (KYC nếu cần OCR post-v1): BullMQ Flows — defer trừ khi roadmap rõ <br><br>**Worker deployment**: **DEFER Sprint 4 (Phase 4 DevOps)**. V1 dev tạm chạy worker **in-process** cùng NestJS API cho ship code nhanh; sau khi Sprint 4 chốt container/deploy target → refactor sang **separate worker process** (`apps/api/src/main.ts` + `apps/api/src/worker.ts` cùng codebase) per ADR-002 prep. Lý do defer: pattern in-process vs separate đều dùng cùng BullMQ API, refactor 1-2 ngày khi cần. <br><br>**Workflow engine cho KYC**: v1 KYC = **manual admin review** (FR-KYC) — không cần Temporal. BullMQ Flows cover được multi-step nếu sau cần (OCR → verify → notify chain). Defer Temporal sang post-v1 nếu workflow demand thực sự nặng. <br><br>**Outbox pattern** (booking commit + queue.add atomic): **Best-effort v1** — `queue.add` chạy sau `prisma.booking.create` thành công (latency ~5ms, hiếm fail). Nếu production đo được "ghost booking" (booking thành công nhưng SMS không gửi do queue.add fail), refactor sang pg outbox table + cron poll publish — LLD Sprint 5-6. |
| Alternatives đã xét | (1) **pg-boss** (Postgres-based) — 0 infra mới + booking transaction atomic outbox free + cost thấp nhất. Yếu: AI assist + NestJS integration (`nest-pg-boss` community nhỏ) yếu hơn BullMQ; DB connection pool consume cho queue throughput cao; dashboard built-in đơn giản. Khanh đã commit Redis Layer 8 → không có lý do từ chối BullMQ. (2) **Temporal** (workflow engine) — workflow-first design, sweet spot KYC long-running, dashboard mạnh nhất. Yếu: **Khanh nghiệp dư + Temporal API mới = mismatch**; +1 cluster phải vận hành (self-host) hoặc Temporal Cloud $200+/mo; AI assist mỏng. Overkill marketplace v1. Defer post-v1 nếu workflow demand. (3) **Cloud queue** (AWS SQS / GCP Tasks / Azure SB) — managed, không ops infra. Yếu: vendor lock SDK + region latency + IAM; quyết định phụ thuộc Layer 11 (deploy target) chưa có context. Defer không hợp lý khi BullMQ là free upgrade Redis Layer 8. (4) **Bull v3 (legacy via `@nestjs/bull`)** — predecessor của BullMQ, JS-based, không có Flows/rate limiter v2 — không có lý do chọn predecessor. (5) **RabbitMQ** — AMQP traditional, +1 broker infra phải vận hành, overkill v1. |
| Hệ quả tích cực | (a) **0 infra mới** — reuse Redis Upstash Layer 8. (b) **NestJS chính chủ** `@nestjs/bullmq` — decorator `@Processor` + `@Process` giống NestJS controller pattern Khanh đã quen Layer 3. (c) **AI assist top tier** — BullMQ là queue phổ biến nhất Node 2024-2026, Claude/Copilot/Stack Overflow đều có context. (d) **Cover 100% use case marketplace v1** — fan-out + cron repeat + exponential retry + DLQ + multi-step Flows. (e) **Bull Board open-source dashboard** mount endpoint `/admin/queues` → Khanh xem job pending/active/completed/failed bằng UI, không cần CLI Redis. (f) **Cron syntax native** — payout T+3 = 1 dòng config `repeat: { pattern: '0 0 * * *' }`. (g) **DLQ + retry exponential built-in** — production-grade từ đầu, không DIY. (h) **Strangler-ready** — module by module refactor sang Temporal post-v1 nếu workflow demand cụ thể (vd KYC OCR phức tạp). (i) **Worker scale độc lập** post-Sprint 4 — refactor sang separate process pattern khi container/deploy chốt; BullMQ API không đổi. (j) **Cost** — không thêm cost vendor, chỉ tăng Redis req volume marginally; Upstash Pro tier $5-15/mo cover được. |
| Hệ quả tiêu cực & mitigation | (1) **Redis SPoF** mở rộng từ cache+lock sang queue → mitigate: ADR-015 đã chốt fallback policy (503 user retry, Upstash 99.95% uptime); queue job persist trong Redis với AOF (Upstash mặc định bật), không mất khi restart. (2) **Outbox pattern best-effort v1** (booking commit + queue.add không atomic) → mitigate: `queue.add` latency ~5ms hiếm fail; nếu production đo "ghost booking" → refactor pg outbox table LLD Sprint 5-6. (3) **Long-running job > 30 min** (vd KYC OCR future) → mitigate: BullMQ "stalled job" detection + heartbeat extend; nếu workflow demand thực sự nặng post-v1, refactor sang Temporal. (4) **Worker in-process v1 risk block event loop** với heavy job (payout aggregate) → mitigate: v1 traffic + job mix chủ yếu light job (SMS/email/audit < 1s); heavy job (payout T+3) chạy 00:00 traffic thấp, acceptable temporary block; refactor separate worker Sprint 4 trước khi traffic tăng. (5) **Dashboard authentication** Bull Board → mitigate: mount qua NestJS guard role `PlatformAdmin` only; HTTPS-only endpoint. (6) **Cron payout T+3 risk multi-worker run** (2 worker pull cùng job) → mitigate: BullMQ rate limiter + concurrency 1 cho payout queue + idempotent job design (check `payout_cycle` table state); ADR-015 distributed lock cũng có thể wrap thêm `payout-cycle:{date}` lock. (7) **Job version migration** (đổi job payload schema sau v1) → mitigate: version field trong payload + handler check version; document migration pattern LLD Sprint 5-6. |
| Nguồn | Sprint 2 Layer 9 workshop (26/05/2026); 4 candidate × 12 axes matrix + concept primer + 10 use case mapping; Khanh chốt "A. BullMQ + @nestjs/bullmq trên Redis Upstash (Recommended)" + "Worker deployment defer Sprint 4 Phase 4 DevOps". |

### ADR-017. Auth library + identity model v1: Better Auth + custom NestJS adapter, Account-separate per scope, Platform-controlled provisioning

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (26/05/2026 — Sprint 2 Layer 10 workshop output, 2 vòng) |
| Bối cảnh | Layer 10 sau ADR-015/016 (Redis Upstash + BullMQ). Marketplace v1 có **8 actor type** (Anonymous, Passenger, Operator Owner, Driver, TicketStaff, SupportStaff, PlatformAdmin, PlatformSupport) × sensitivity từ Low (Anonymous) đến Critical (PlatformAdmin). Khanh self-rate "nghiệp dư từ Layer 8". Workshop chia 2 vòng. **Vòng 1**: library choice + token model + OAuth provider → Khanh "[No preference]" cả 3 sub-decision → AI áp dụng Recommended defaults và document rõ. **Vòng 2**: Khanh challenge model "Email = identifier Operator" raise 3 vấn đề critical: (1) **1 Operator entity có thể có nhiều người owner** → email cá nhân 1 người KHÔNG phải identifier Operator; (2) **Login identifier cần Platform admin control**, không cho self-register Operator bằng email tự do; (3) **Vấn đề tương tự với Platform Admin** — Platform Admin cũng phải Platform-controlled provisioning. Reframe model → tách 3 khái niệm Entity/Identity/Account; Identifier model 3 namespace; Provisioning Platform-controlled. |
| Quyết định | V1 dùng **Better Auth** (TypeScript-first auth lib, framework-agnostic, MIT license, launched 2024) + **custom NestJS adapter** wrap Express middleware vào NestJS interceptor pattern (Better Auth chưa có official `@nestjs` module — work ~2-3 ngày LLD Sprint 5-6).<br><br>**Identifier model — 3 namespace tách biệt**:<br>• **Passenger** = Email (RFC 5321 unique) — login Email OTP primary, Email+Password optional sau khi link payment method<br>• **Operator-side user** = `{operatorSlug}/{username}` (vd `phuongtrang/owner01`, `phuongtrang/driver042`) — slash separator (Q1=A); `operatorSlug` do Platform admin issue khi approve KYC, immutable; `username` do Operator owner issue trong scope qua Operator OS dashboard<br>• **Platform-side user** = `platform/{username}` (vd `platform/khanh`, `platform/admin01`) — super-admin (Khanh) issue qua super-admin panel<br><br>**Identity model — Account separate (Q2=α)**: mỗi `(scope, username)` pair là **account riêng**, KHÔNG federate. 1 người multi-role = N account separate (N password, N TOTP). Lý do: 95% user single-role v1, audit log clean, multi-tenant guard đơn giản. Migrate sang federated identity (β) post-v1 nếu sales feedback yêu cầu.<br><br>**Provisioning model — Closed enrollment (Q3=A)**:<br>• **Operator onboarding**: public form "Đăng ký trở thành Operator" (KYC docs + bank info + contact email + người đại diện) → Platform admin review KYC → approve issue `operatorSlug` + first Owner account `{slug}/owner01` + temporary password 1-time → send tới contact email → first Owner login force change password + setup TOTP mandatory → Operator status `Active`. **Operator self-register bằng email tự động TUYỆT ĐỐI không cho phép v1.**<br>• **Operator employee** (co-Owner / Driver / TicketStaff / SupportStaff): Operator owner qua Operator OS dashboard issue `{slug}/{username}` + temp password + role → employee first login force change password.<br>• **Platform employee**: super-admin (Khanh) qua super-admin panel issue `platform/{username}` + temp password + role.<br>• **Passenger**: self-register Email + verify Email OTP — only open enrollment v1.<br><br>**Login method matrix per actor**:<br>• Anonymous: N/A (browse only)<br>• Passenger: Email + OTP (primary); Email+Password optional sau khi link payment; Biometric mobile re-auth mỗi 7 ngày<br>• Operator Owner: `{slug}/{username}` + Password + **TOTP mandatory** + backup code 10 single-use<br>• Operator Driver/TicketStaff/SupportStaff: `{slug}/{username}` + Password + TOTP optional v1 (recommend mandatory Sprint 5-6); Biometric mobile mỗi ca<br>• Platform Admin: `platform/{username}` + Password + **TOTP mandatory** + IP allowlist (post-v1)<br>• Platform Support: `platform/{username}` + Password + **TOTP mandatory**<br><br>**Token model — Hybrid JWT + opaque refresh** (Recommended default vòng 1):<br>• **Access token**: JWT RS256, TTL **15 phút**, payload `{userId, scope: 'operator' \| 'platform' \| 'passenger', operatorSlug?, username, role, sessionId}` — KHÔNG chứa permission detail (lookup Redis cache via Layer 8); httpOnly + SameSite=Lax cookie cho Web, `expo-secure-store` Keychain/Keystore cho Mobile<br>• **Refresh token**: opaque random 32-byte, TTL **30 ngày**, persist Postgres `auth_session` table, **rotation mỗi lần refresh** (one-time use), **family invalidation** nếu detect token reuse (attack signal — invalidate cả token family), revoke ngay khi user logout / admin force / Operator suspend<br>• Session metadata cache Redis (ADR-015) TTL 15 phút; lookup hot path không hit Postgres.<br><br>**Multi-tenant guard**: NestJS `TenantGuard` extract `operatorSlug` từ JWT claims + match request param `:operatorSlug` (URL pattern `/v1/operators/:slug/...`) — mismatch = 403 Forbidden. **Defense-in-depth qua Postgres RLS** (ADR-011) ở DB level — query phải `SET LOCAL app.operator_slug = '...'` cho mỗi transaction; RLS policy `USING (operator_slug = current_setting('app.operator_slug'))`.<br><br>**Permission model**: **RBAC v1** — 8 role hardcoded enum (Anonymous / Passenger / OperatorOwner / Driver / TicketStaff / SupportStaff / PlatformAdmin / PlatformSupport). Permission matrix per resource × action document chi tiết trong 07 Security §6 rework Sprint 5-6. **ABAC defer post-v1** (migration path: thêm context field vào permission check function `can(user, action, resource, context)`).<br><br>**OAuth provider v1**: **DEFER Sprint 3 Phase 3 Layer 13** — Zalo / Google / Facebook decision đi cùng OQ-09 (notification provider). V1 launch chỉ Email OTP + Password đủ cover 80% use case VN passenger. |
| Alternatives đã xét | **Auth library** (5 candidate × 14 axes vòng 1): (1) **Auth.js v5 (NextAuth)** — mature 2018+, ecosystem lớn nhất (25k+ stars), nhưng **Next-centric** (NestJS BE adapter không official, community adapter chất lượng không ổn định), multi-tenant DIY phức tạp, namespace scope (slug/username) phải custom hoá deep. Loại do mismatch monorepo NestJS BE + Next FE + Expo Mobile cần auth shared. (2) **Passport + @nestjs/passport** — DIY mọi thứ (OTP flow, MFA TOTP, refresh rotation, organizations, slug-scoped namespace, family invalidation) → time-to-v1 +2 sprint vs Better Auth, high security risk amateur. Loại do scope. (3) **Clerk** (managed SaaS) — UI components prebuilt + MFA built-in + organizations native + multi-tenant out-of-box + free tier 10k MAU. Nhưng **vendor lock** (auth state Clerk DB không export dễ), không có Zalo native (chỉ Google/Apple/Email/SMS qua Twilio), data residency US (compliance VN concern), cost $25-100/mo sau free tier. **Slug-scoped username pattern KHÔNG fit** Clerk organizations model (Clerk hardcoded email = primary identifier per user). Loại do model mismatch với Khanh Q1/Q2/Q3. (4) **Supabase Auth** — auth built-in Supabase platform. Chỉ make sense nếu Layer 4 vendor = Supabase. ADR-011 chốt Postgres self-host/Neon + Prisma 5 (KHÔNG Supabase) → Supabase Auth standalone không có lợi gì so với DIY. Loại do dependency Layer 4. (5) **Lucia v3** — maintainer Pilcrow deprecated end-2024, repo marked archive. Loại từ đầu vòng 1.<br><br>**Identifier model alternatives** (Q1): (a) Dot `phuongtrang.owner01` — đỡ confuse URL path nhưng dễ confuse hostname pattern. (b) At-sign `owner01@phuongtrang` — giống email gây nhầm field, parser conflict với email validation. (c) Numeric `OPR-001234-USR-005` — tightest control, anti-enumeration mạnh nhất, nhưng UX khó nhớ cho human-facing login form. Khanh chọn A (slash).<br><br>**Identity model alternatives** (Q2): **β. Federated identity** (Slack / Google Workspace pattern) — 1 `identityId` Platform-level link N `(scope, username, role)` accounts; 1 password + 1 TOTP, chọn workspace sau Step 1 login. Defer post-v1 (Khanh chốt α): pros UX tốt cho multi-role nhưng implement phức tạp + multi-tenant guard phải thêm layer identity↔account resolution; 95% user single-role v1 không justify complexity.<br><br>**Provisioning alternatives** (Q3): **B. Open self-register + KYC sau** — Operator tự register Email contact, system tự tạo slug + first Owner; sau đó Platform admin review KYC, deactivate nếu không pass. Loại do mất control onboarding + risk spam Operator + branding risk (slug có thể bị squat). **C. Hybrid sandbox + Active sau approve** — self-register tạo Pending Operator, có thể login sandbox môi trường test trước; chuyển Active sau Platform approve. Defer post-v1 nếu sales pipeline cần demo flow.<br><br>**Token model alternatives**: (i) **JWT only stateless** — không refresh DB, mọi logout phải đợi access token expire. Yếu: không revoke ngay khi cần (admin force logout user vi phạm phải đợi 15min, không acceptable cho marketplace có tiền). (ii) **Opaque only DB-backed** — mọi request hit DB session lookup. Yếu: hot path API mỗi request +5ms DB query, scale tệ; Redis cache help nhưng cache invalidation phức tạp. (iii) **Defer token model** — không khả thi vì auth là blocker mọi endpoint. |
| Hệ quả tích cực | (a) **Better Auth framework-agnostic** → NestJS BE + Next Web + Expo Mobile cùng dùng được; share Zod schemas Layer 3. (b) **Slug-scoped namespace** rõ scope multi-tenant ngay từ identifier — không nhầm Operator A user với Operator B user; debug log/audit trail đọc dễ. (c) **Email KHÔNG phải login identifier Operator-side** → Operator entity decoupled khỏi email cá nhân; nhân viên rời cty không invalidate Operator entity; co-owner setup không conflict. (d) **Platform-controlled provisioning** = Platform admin có toàn quyền block/issue/revoke Operator + Platform employee, không có self-register backdoor; KYC gate trước Active. (e) **Account separate v1** = audit log clean (mỗi action ghi `(scope, username)` đủ), multi-tenant guard đơn giản (extract slug từ JWT claims), không có federated identity edge case. (f) **Hybrid token** = stateless JWT cho hot path API (90% req không hit DB), revocable refresh cho security; admin force logout user vi phạm tối đa 15min đợi access expire. (g) **Token rotation + family invalidation** = chống token replay/theft attack pattern (industry standard 2024+). (h) **TOTP mandatory cho Owner/Admin/Support** = compliance baseline đúng (PCI-DSS, ISO 27001 SC); backup code 10 single-use cover lost device. (i) **OAuth defer Sprint 3** = không khóa v1 launch, VN passenger primary Email OTP đủ; Zalo/Google quyết cùng notification provider OQ-09. (j) **Multi-tenant defense-in-depth** = app guard (TenantGuard) + DB RLS (ADR-011) — 2 lớp bảo vệ, app layer bug không leak cross-tenant data. (k) **RBAC 8 role v1 simple** đủ cover, không over-engineer ABAC; migration path rõ ràng nếu cần. (l) **TypeScript + Zod align stack** Layer 2/3/5. (m) **MIT license + active maintainer 2024-2026** Better Auth (monitor + abstraction layer mitigate maint risk). |
| Hệ quả tiêu cực & mitigation | (1) **Better Auth là lib mới (launched 2024)** → cộng đồng nhỏ hơn Auth.js (10k+ stars vs 25k+); risk maintainer drop → mitigate: monitor maintainer activity monthly; LLD Sprint 5-6 wrap Better Auth API trong abstraction layer `packages/auth-core/` để migrate sang Auth.js hoặc DIY trong 1-2 tuần nếu cần. (2) **Custom NestJS adapter** (Better Auth chưa có official NestJS module) → mitigate: pattern wrap Express middleware → NestJS interceptor là chuẩn, AI assist gen được, work ~2-3 ngày LLD Sprint 5-6. (3) **Account separate edge case** multi-role user (vd Khanh dogfood = Platform Admin + Owner test Operator) phải nhớ N password → mitigate: 95% user single-role v1; multi-role user có thể dùng password manager (1Password/Bitwarden); migrate sang federated β post-v1 nếu thực sự cần. (4) **Solo Owner mất TOTP + email** → recovery khó → mitigate: Platform admin manual recovery procedure (verify KYC docs gốc + contact phone đăng ký + có thể yêu cầu video call); document trong 07 Security §5 rework Sprint 5-6; UI prompt mạnh khi setup Operator khuyến nghị có ≥ 2 Owner để tránh single point of failure. (5) **Closed enrollment làm chậm Operator onboarding** (1-3 ngày Platform admin review KYC) → mitigate: trade-off acceptable v1 (chất lượng > tốc độ); KYC OCR automation roadmap post-v1 giảm xuống vài giờ. (6) **Refresh token DB-backed** thêm Postgres query mỗi 15min refresh → mitigate: refresh interval thấp tổng thể (96 lần/day/user worst case nếu auto refresh active session); Postgres index `auth_session.token_hash` + Redis cache session metadata Layer 8. (7) **TOTP mandatory friction onboarding Owner** → mitigate: setup wizard step-by-step với screenshot + video tutorial; backup code 10 single-use cấp ngay setup; FAQ + support escalation. (8) **operatorSlug immutable** = Operator merger/rename phải giữ slug cũ → mitigate: display name có thể đổi qua Platform admin approval; slug chỉ là technical identifier không hiển thị marketplace public (chỉ trong operator-os URL nội bộ); document FAQ. (9) **Permission RBAC granularity** có thể không đủ tương lai (vd Owner muốn delegate "view payout report" cho 1 nhân viên mà không cấp role SupportStaff đầy đủ) → mitigate: ABAC migration path qua context field trong permission check function `can(user, action, resource, context)`; document trong 07 Security rework. (10) **Slash separator `/` trong identifier** có thể conflict URL path parser nếu không escape → mitigate: identifier truyền qua POST body hoặc query string, KHÔNG URL path; document encoding LLD Sprint 5-6. (11) **Khanh "[No preference]" vòng 1 cho auth lib + token + OAuth** + Khanh challenge model email vòng 2 → quyết định có 2 phần (Recommended default vòng 1 + Khanh-driven reframe vòng 2) → cần **CRITICAL re-confirm** khi 07 Security §5/§6 rework Sprint 5-6 + LLD Sprint 5-6 → mitigate: đánh dấu rõ "**CRITICAL re-confirm**" trong ADR Nguồn + checklist rework Sprint 5-6. |
| Nguồn | Sprint 2 Layer 10 workshop (26/05/2026), 2 vòng. **Vòng 1**: 5 candidate auth lib (Better Auth / Auth.js v5 / Passport+NestJS / Clerk / Supabase Auth — Lucia v3 loại deprecated) × 14 axes + 3 sub-decision (lib / token / OAuth) qua AskUserQuestion → Khanh "[No preference]" cả 3 → AI áp dụng Recommended defaults (Better Auth + Hybrid JWT 15min access + opaque refresh 30 ngày rotation+family invalidation + Defer OAuth Sprint 3). **Vòng 2**: Khanh raise 3 challenges critical: (1) 1 Operator có nhiều owner, (2) Login identifier Platform-controlled, (3) Platform Admin vấn đề tương tự. AI reframe Entity/Identity/Account 3 namespace + Closed enrollment. Khanh chốt **Q1=A** (`{operatorSlug}/{username}` slash separator), **Q2=α** (Account separate v1 không federate), **Q3=A** (Closed enrollment Platform-controlled, Operator/Admin không self-register). **CRITICAL re-confirm khi 07 Security §5/§6 rework Sprint 5-6 + LLD Sprint 5-6**. |

### ADR-018. Object Storage v1: Cloudflare R2 + Storage adapter (KYC dev-local / production-deferred)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (26/05/2026 — Sprint 2 Layer 11 workshop output, Sprint 2 closing layer) |
| Bối cảnh | Layer 11 closing Sprint 2 Phase 2 Infrastructure. Sau Layer 1-10. Marketplace cần lưu **file nhị phân**: KYC docs (FR-KYC — ĐKKD, giấy phép vận tải, CCCD đại diện, bank info; private, sensitive), operator logo/cover (public), vehicle photo/seat layout (public), dispute evidence (private), avatar (semi), report export (private temp), payment/refund proof (private sensitive). E-ticket QR gen on-the-fly không cần lưu file. Đặc điểm: mix public+private; KYC+payment = **nhạy cảm cao** (dữ liệu cá nhân VN); public assets cần CDN → **egress cost** là yếu tố lớn; volume v1 nhỏ-vừa (vài chục GB). Khanh nghiệp dư → ưu tiên managed + S3-compatible + AI assist. **Compliance flag**: Nghị định 13/2023 (PDPD — cross-border transfer dữ liệu cá nhân cần DPIA) + Nghị định 53/2022 (data residency tại VN). Workshop 5 option × 12 axes + concept primer + use case mapping + compliance flag. |
| Quyết định | **Provider chính v1: Cloudflare R2** (object storage S3-compatible, egress $0, CDN Cloudflare built-in).<br><br>**Stack TS/NestJS**:<br>• Driver: `@aws-sdk/client-s3` trỏ R2 S3-compatible endpoint (KHÔNG dùng SDK proprietary → migrate S3/B2 dễ)<br>• `@aws-sdk/s3-request-presigner` cho presigned URL<br>• Storage abstraction: `StorageService` port + adapter pattern (consistent **ADR-006** storage adapter boundary)<br><br>**Bucket layout — 2 tier**:<br>• `vexenhanh-public` — operator logo, vehicle photo, avatar (public-read + Cloudflare CDN cache edge gần VN)<br>• `vexenhanh-private` — dispute evidence, report export, KYC, payment proof (private; presigned URL only; access audit log Mongo per CLAUDE.md security)<br><br>**Presigned URL**: upload + download qua presigned URL TTL ngắn (5 phút); KYC/payment download audit log mỗi access.<br><br>**KYC + payment-proof (sensitive personal data) — chiến lược theo giai đoạn (Khanh Q2)**:<br>• **Dev/test**: `LocalStorageAdapter` (local filesystem `./storage/` hoặc MinIO container S3-compatible) — KHÔNG dùng KYC data thật → tránh compliance trong dev, velocity cao.<br>• **Production**: **DEFER — phát triển tiếp khi chuẩn bị production launch thật**. Data residency strategy cuối (VN local split-bucket vs R2 + DPIA Nghị định 13/2023 cross-border) quyết khi có data thật + context production. **Blocker trước go-live production** (KHÔNG block MVP/demo). Track qua **OQ-21** + **ADR-OQ-04**.<br><br>**Adapter swap**: `StorageService` interface cho phép dev/test (local) ↔ production R2 ↔ KYC production (TBD VN-local) swap qua env config, không đổi business code.<br><br>**Lifecycle**: report export auto-delete 7 ngày; KYC/payment retention policy defer 07 Security rework Sprint 5-6.<br><br>**CDN**: public bucket qua Cloudflare CDN cache logo/vehicle ở edge gần VN. |
| Alternatives đã xét | (1) **AWS S3** (SG ap-southeast-1) — chuẩn de-facto, ecosystem + AI assist mạnh nhất, presigned URL gốc. Yếu: **egress $0.09-0.12/GB đắt** — marketplace serve nhiều ảnh public (logo/vehicle trang search) → bill băng thông tăng nhanh; CloudFront CDN phải cấu hình riêng. R2 thắng vì egress free + CDN built-in, vẫn S3-compatible nên giữ ecosystem. (2) **Backblaze B2** — storage rẻ nhất $0.006/GB, egress free qua Cloudflare Bandwidth Alliance. Yếu: **region chỉ US/EU, không SG/VN** → latency cao từ VN. (3) **VN local (Viettel IDC / VNG Cloud / FPT)** — data residency VN tốt nhất (compliance Nghị định 13/2023 + 53/2022). Yếu: cost cao hơn, ecosystem + AI assist yếu, S3-compatibility tùy provider, time-to-v1 chậm (hợp đồng). **Giữ làm option cho KYC production storage (OQ-21)**. (4) **Self-host MinIO** (S3-compatible open-source VPS) — full control + data residency nếu VPS ở VN. Yếu: ops burden cao (backup/scale/uptime/patch) không hợp Khanh nghiệp dư; dùng làm **local dev adapter** thì OK. (5) **Supabase Storage** — chỉ value nếu Layer 4 = Supabase; ADR-011 chốt Postgres self-host/Neon → loại.<br><br>**Compliance strategy alternatives** (Q2): A. Defer all + DPIA trước production. B. Split bucket ngay (KYC VN local + còn lại R2). C. All VN local. **Khanh chốt biến thể của A: dev/test KYC = local storage, production KYC = defer phát triển tiếp** — đơn giản dev (không data thật), quyết compliance khi context production rõ. |
| Hệ quả tích cực | (a) **Egress $0 R2** = serve ảnh public không lo bill băng thông spike → cost predictable. (b) **S3-compatible** → `@aws-sdk/client-s3` chuẩn, AI assist tốt, Strangler-ready (swap S3/B2/VN-local đổi endpoint config). (c) **CDN Cloudflare built-in** = logo/vehicle cache edge gần VN, không cấu hình CloudFront. (d) **Storage adapter (ADR-006)** = dev local ↔ production R2 ↔ KYC VN-local swap không đổi business code; test không cần network/cloud. (e) **Dev KYC local** = không vướng compliance trong dev (không data thật), velocity cao. (f) **Production KYC defer hợp lý** = volume v1 nhỏ, không premature optimize compliance; quyết khi có data thật. (g) **Presigned URL** = client tải thẳng storage, không proxy qua API server (tiết kiệm băng thông app); KYC/payment + audit log mỗi access. (h) **Managed R2 ops thấp** hợp Khanh nghiệp dư. (i) **2 bucket public/private** tách visibility rõ, giảm risk lộ KYC. (j) **Cost v1 thấp** — vài chục GB × $0.015/GB + egress free ≈ vài $/tháng. |
| Hệ quả tiêu cực & mitigation | (1) **KYC production storage chưa quyết** → risk compliance khi go-live → mitigate: track **OQ-21 + ADR-OQ-04 là blocker trước production launch thật**; dev/test local không vướng (không data thật). (2) **R2 nước ngoài** cho non-KYC private (dispute evidence có thể chứa PII) → mitigate: dispute evidence v1 ít PII nhạy cảm; nếu cần đưa vào scope OQ-21 production review. (3) **Cloudflare vendor lock** → mitigate: chỉ dùng S3-compatible API qua `@aws-sdk/client-s3`, KHÔNG dùng R2 proprietary feature; swap S3/B2 đổi endpoint env. (4) **Local dev adapter khác production R2** (filesystem semantics ≠ object storage) → mitigate: dùng MinIO container cho dev (S3-compatible giống R2) để parity nếu cần; hoặc dev point R2 dev bucket. (5) **Presigned URL leak** (URL bị share) → mitigate: TTL ngắn 5min + audit log; KYC download log mỗi access. (6) **Egress free R2 fair-use** (Cloudflare có thể throttle abuse) → mitigate: volume v1 thấp xa ngưỡng; monitor; CDN cache giảm origin egress. (7) **2 provider khi production KYC = VN local** → quản 2 storage → mitigate: adapter abstraction giấu complexity; chỉ KYC/payment bucket khác provider. |
| Nguồn | Sprint 2 Layer 11 workshop (26/05/2026), Sprint 2 closing layer; 5 option (Cloudflare R2 / AWS S3 / Backblaze B2 / VN local / MinIO) × 12 axes matrix + concept primer (object storage vs DB/filesystem, bucket/object/key/presigned URL/CDN/lifecycle/encryption) + use case mapping 8 loại file × public/private/sensitivity + compliance flag (Nghị định 13/2023 PDPD + 53/2022 data residency). Khanh chốt **Q1=A** (Cloudflare R2 provider chính) + **Q2** (KYC + payment-proof dev/test = local storage; production = defer phát triển tiếp, track OQ-21). Close **SEC-OQ-04** (object storage provider). Consistent **ADR-006** (storage adapter boundary). **Sprint 2 CLOSED — 4/4 layer Phase 2 Infrastructure.** |

---

### ADR-019. Payment Gateway v1: VNPay primary + MoMo phương thức 2 (đa phương thức v1), sau `PaymentGateway` adapter

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (31/05/2026 — Sprint 3 Layer 12 workshop output, Sprint 3 opening layer, Phase 3 VN vendor) |
| Bối cảnh | Layer 12 mở Phase 3 VN vendor sau khi Sprint 2 đóng Phase 2 Infrastructure. **Marketplace vế THU**: hành khách trả tiền vé qua cổng → tiền về tài khoản merchant ngân hàng Platform → Platform giữ escrow → payout Operator T+3 (Layer 15 sẽ chốt). Layer 12 chỉ lo vế THU. **Ràng buộc đã khóa**: escrow model (ADR-005, AS-17, FR-BTP-15) Platform giữ tiền trong tài khoản escrow, payout T+3; adapter mandated (AS-08, NFR-COMP-05, ADR-006) — provider sau `PaymentGateway` port; pay-first only v1 (OQ-07); VND-only, VN-only (OQ-12); webhook idempotent + HMAC verify (FR-BTP-08, NFR-DATA-04); xử lý async qua BullMQ Layer 9 (NFR-PERF-04); refund API (OQ-13); đối soát 2 chiều theo provider txn id (FR-BTP-17, NFR-DATA-03). **Khách bus trải khắp VN nhiều lứa tuổi** → cần phủ thẻ ATM/napas + QR + ví; không thể chỉ ví đô thị. **Khanh nghiệp dư hạ tầng thanh toán** → ưu tiên redirect/hosted (PCI SAQ-A nhẹ), AI assist tốt, sandbox sẵn. **Cờ pháp lý critical**: escrow Platform giữ tiền hành khách + chi cho Operator = hoạt động trung gian thanh toán theo **Nghị định 52/2024** (thay 101/2012) → production cần **NHNN license** hoặc đi qua bên licensed. Workshop 5 option + 1 note × 7 axes + primer + compliance flag. |
| Quyết định | **Primary v1: VNPay** (provider chính, đáp ứng AS-08 sandbox; phủ napas ~40 bank + QR + thẻ quốc tế qua 1 tích hợp).<br><br>**Phương thức 2 v1: MoMo** (đa phương thức ngay v1, Khanh chốt Q2 — phủ ví 30M user + thẻ/bank fallback).<br><br>**Stack TS/NestJS**:<br>• `PaymentGateway` port (interface) ở `external/payment/`<br>• `VnpayAdapter` ở `external/payment/vnpay/` — HTTP + HMAC-SHA512 sign/verify, redirect URL build, IPN handler, querydr API, refund API<br>• `MomoAdapter` ở `external/payment/momo/` — HTTP + HMAC-SHA256, AIO redirect/QR/deeplink, IPN handler, refund API<br>• Webhook handler chung: verify HMAC theo provider → dedup theo `(provider, providerTxnId)` (idempotent FR-BTP-08) → enqueue BullMQ job xử lý async (NFR-PERF-04) → cập nhật `Payment` + `EscrowLedger` trong cùng Postgres transaction<br>• Reconciliation job (BullMQ scheduled cron daily) call querydr API mỗi provider → đối chiếu với DB `Payment` → flag RECONCILING nếu lệch (FR-BTP-17, NFR-DATA-03)<br><br>**Escrow giữ in-house ledger** (`EscrowLedger` entity per ADR-005), độc lập với cổng. Cổng chỉ thu hộ về tài khoản merchant Platform; Platform tự ghi sổ ledger.<br><br>**Refund**: gọi refund API của provider gốc (`Payment.providerTxnId` xác định cổng). Trạng thái Refund: PENDING / PROCESSING / SUCCESS / FAILED + manual escalate admin nếu fail (OQ-13).<br><br>**Settlement**: tiền về tài khoản merchant ngân hàng Platform theo cycle từng cổng (VNPay T+1, MoMo T+1 — confirm khi sign HĐ).<br><br>**PCI scope**: cả 2 redirect/hosted → SAQ-A; Platform KHÔNG bao giờ chạm dữ liệu thẻ.<br><br>**Compliance — giấy phép TGTT NHNN (Khanh Q3)**:<br>• MVP/sandbox: không vướng (không tiền thật)<br>• Production: **blocker pháp lý/cấu trúc** — track **OQ-22** + **ADR-OQ-05**; 3 hướng (a) Platform tự xin license, (b) cổng licensed (VNPay/MoMo đều là tổ chức TGTT) giữ + settlement, escrow = sổ kế toán trên merchant account Platform, (c) thu hộ/chi hộ qua đối tác licensed.<br><br>**Loại khỏi v1**: Stripe (không onboarding merchant VN, không settle VND OQ-12; Stripe Connect là tham chiếu mô hình escrow ta tự làm in-house). VietQR (PayOS/Casso/SePay) defer v1.x là option mở rộng phương thức 3 nếu cần phí thấp.<br><br>**Khanh quyết định**: Q2 (chốt trực tiếp) = VNPay+MoMo. **Q1 (primary)** + **Q3 (OQ giấy phép TGTT)** Khanh trao AI áp dụng recommendation mục E của prep → Q1=A VNPay primary, Q3=A raise OQ-22. **CRITICAL re-confirm Sprint 5-6** do Q1+Q3 AI tự áp (tương tự pattern ADR-017 token model + OAuth defer). |
| Alternatives đã xét | (1) **VNPay only v1** — đơn giản nhất, 1 cổng đủ phủ napas+QR+thẻ. Yếu: thiếu ví (MoMo 30M wallet user, khách trẻ/đô thị quen ví) → conversion thấp ở nhóm này. Khanh chọn đa phương thức v1 (Q2). (2) **MoMo as primary** — DX modern, doc tiếng Anh tốt, ví user lớn. Yếu: ví-centric, fallback thẻ qua MoMo gateway nhưng không rộng như napas trực tiếp VNPay; với khách bus xa lứa tuổi rộng → phủ kém hơn. (3) **ZaloPay** — synergy nếu Layer 13 OAuth = Zalo, nhưng ví ZaloPay nhỏ hơn MoMo; ecosystem sale yếu hơn. Defer. (4) **OnePay** (MPGS, dùng cho hàng không/enterprise) — thẻ nội địa+quốc tế mạnh, bank-grade. Yếu: onboarding nặng, ít community, AI assist thấp, không hợp solo+AI MVP. (5) **Stripe + Stripe Connect** — chuẩn vàng marketplace escrow/split/payout. **Loại cứng**: Stripe không hỗ trợ doanh nghiệp đặt tại VN, không settle VND (OQ-12). Giữ trong analysis vì (a) Connect là mô hình tham chiếu cho escrow ta tự làm in-house (ADR-005), (b) relevant nếu sau quốc tế hóa. (6) **VietQR (PayOS/Casso/SePay)** — chuyển khoản bank QR auto-reconcile, phí rất thấp. Yếu: chuyển khoản (không "thanh toán" theo nghĩa gateway), reconciliation async, không phù pay-first instant (OQ-07). Defer v1.x option mở rộng. |
| Hệ quả tích cực | (a) **Phủ khách rộng** — VNPay (napas ~40 bank + QR + thẻ quốc tế) + MoMo (ví 30M + thẻ/bank fallback) cover hầu hết khách bus VN. (b) **Sandbox sẵn cả 2** — Khanh có thể tích hợp song song không chờ HĐ. (c) **Adapter pattern** (ADR-006/AS-08) — thêm/đổi cổng không phá business code; SRS-mandated. (d) **PCI scope nhẹ** — cả 2 redirect/hosted → SAQ-A, Platform không lưu thẻ. (e) **Webhook async qua BullMQ** (Layer 9) — không nghẽn luồng booking, exponential retry + DLQ free; đáp ứng NFR-PERF-04, NFR-AVAIL-02. (f) **Escrow in-house** (ADR-005) — không phụ thuộc cổng cho ledger; gateway chỉ thu hộ. (g) **Reconciliation 2 chiều** qua querydr API + scheduled BullMQ cron — đáp ứng FR-BTP-17, NFR-DATA-03. (h) **AI assist đủ** — cả VNPay + MoMo có nhiều ví dụ TS/Node community VN; HMAC + HTTP là pattern phổ biến. (i) **Strangler-ready** — đổi sang provider khác (ZaloPay, OnePay, foreign nếu quốc tế hóa) chỉ thêm adapter mới. (j) **Stripe Connect là tham chiếu** cho escrow/split mà ta tự làm in-house — clear mental model. |
| Hệ quả tiêu cực & mitigation | (1) **Giấy phép TGTT NHNN cho production** (escrow = hoạt động TGTT theo Nghị định 52/2024) → risk pháp lý production go-live → mitigate: track **OQ-22 + ADR-OQ-05 là blocker trước production launch thật** (KHÔNG block MVP); 3 hướng (Platform tự xin / cổng licensed giữ+settle / thu hộ-chi hộ qua đối tác); quyết khi có data + traction production. (2) **2 provider = 2 bộ webhook + 2 HMAC khác (SHA512 vs SHA256) + 2 sandbox** → ops + dev overhead → mitigate: `PaymentGateway` port giấu khác biệt, mỗi adapter module riêng, idempotency layer chung dedup theo `(provider, providerTxnId)`. (3) **Provider downtime / callback chậm** (NFR-AVAIL-02) → có thể "ghost payment" → mitigate: hiển thị trạng thái rõ cho khách + querydr reconcile job scheduled (BullMQ cron) đối chiếu DB ↔ provider mỗi ngày; flag RECONCILING khi lệch; admin xử lý qua FR-BTP-17. (4) **Phí giao dịch ~1.1-2%+ tùy phương thức/HĐ** → ảnh hưởng commission 5% (OQ-18) → mitigate: tính **net commission sau phí gateway** trong `CommissionRule`; ghi rõ schema DB + báo cáo. (5) **Vendor lock per provider** (HMAC signature, URL build khác nhau) → mitigate: adapter abstraction giữ business code clean; provider-specific code tập trung trong `external/payment/{vnpay,momo}/`. (6) **MoMo merchant onboarding song song VNPay** → 2 quy trình đăng ký + KYC merchant cho Platform → mitigate: Khanh đăng ký sandbox cả 2 song song ngay khi vào LLD Sprint 5-6. (7) **Refund API điều kiện theo HĐ** (VNPay refund cần bật theo HĐ; MoMo có policy retention) → có thể chậm/giới hạn → mitigate: schema Refund ghi trạng thái PENDING/PROCESSING/SUCCESS/FAILED + manual escalate qua admin (FR-BTP-17, OQ-13). (8) **CRITICAL re-confirm Sprint 5-6** do Khanh trao AI tự áp Q1 (primary VNPay) + Q3 (raise OQ-22) — tương tự pattern ADR-017 token model + OAuth defer → flag re-confirm khi rework 07 Security §5/§6 + LLD payment chi tiết Sprint 5-6 + khi viết schema Refund/Payment ở 04 DB rework. |
| Nguồn | Sprint 3 Layer 12 workshop (31/05/2026), Sprint 3 opening layer, Phase 3 VN vendor; 5 option (VNPay / MoMo / ZaloPay / OnePay / Stripe) + 1 note (VietQR PayOS/Casso/SePay) × 7 axes matrix + primer (redirect/hosted vs API-direct, IPN/webhook nguồn chân lý, querydr/reconcile, settlement, PCI SAQ-A) + cờ pháp lý NHNN Nghị định 52/2024 trung gian thanh toán. Khanh chốt **Q2 = VNPay + MoMo đa phương thức v1**; **Q1 + Q3 trao AI áp recommendation** → Q1=VNPay primary, Q3=raise OQ-22. **Re-close OQ-05** (payment gateway provider for v1, re-opened 25/05/2026). Raise **OQ-22 + ADR-OQ-05 mới** (giấy phép TGTT NHNN production). Consistent **ADR-006** (payment adapter boundary mandated), **ADR-005** (escrow in-house ledger), **AS-08, NFR-COMP-05** (adapter), **FR-BTP-08, NFR-DATA-04** (webhook idempotent), **FR-BTP-17, NFR-DATA-03** (reconciliation), **OQ-07** (pay-first), **OQ-12** (VND/VN), **OQ-13** (refund), **OQ-18** (commission 5% net sau phí). **Sprint 3 1/4 layer chốt — còn Layer 13 Notification (re-close OQ-09 + OAuth per ADR-017), 14 Routing, 15 Bank payout (re-close OQ-16).** |

---

### ADR-020. Notification (Email + SMS + Push) + OAuth provider v1: Resend / SMS defer / Expo Push / Google+Facebook+Apple Passenger-only

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (31/05/2026 — Sprint 3 Layer 13 workshop output, Phase 3 VN vendor) |
| Bối cảnh | Layer 13 gộp 4 sub-decision cùng họ provider-integration trên `NotificationAdapter` port (`external/notification/{email,sms,push}/`) per ADR-006 + OAuth lib stack per ADR-017 Better Auth. **Use case marketplace** (SRS): OTP delivery cho Passenger Email login (ADR-017), booking confirmation 3 kênh fan-out, payment success/fail, refund status, payout notification Operator (T+3), KYC status update, dispute update, trip departure reminder mobile push, employee mobile push (driver schedule, support ticket). **Ràng buộc đã khóa**: Email = OTP primary Passenger (ADR-017, OQ-09 cũ); Push = qua Expo SDK không eject (ADR-014); OAuth chỉ Passenger (ADR-017, Operator/Platform closed enrollment); adapter port mandated (ADR-006); async fan-out qua BullMQ (ADR-016, NFR-PERF-04); OTP idempotent + rate-limit (SEC-OQ-07). **Khanh chốt tiêu chí "miễn phí cho v1"** → AI áp dụng recommendation defaults các sub-decision đều khớp tier free hoặc no marginal cost. Workshop 4 sub-decision matrix + primer + compliance flag. |
| Quyết định | **A. Email = Resend** (primary v1, tier free 3k email/mo).<br>• Stack: `resend` npm SDK + React Email templates (`@react-email/components`) fit Next.js Layer 6<br>• 1 verified domain free tier; DNS SPF/DKIM/DMARC config khi launch<br>• Webhook events (delivered/bounced/opened) → ingest BullMQ Layer 9 → ghi `notification_log` Mongo audit per ADR-011<br>• Template React Email cho transactional (OTP, booking confirm, payment, payout, KYC, dispute)<br>• Khi vượt 3k/mo → upgrade Resend $20/mo 50k hoặc swap adapter sang SendGrid/AWS SES (adapter ready)<br><br>**B. SMS = defer v1** (Hướng α — chỉ adapter port + LocalLoggerSmsAdapter dev).<br>• `SmsNotifier` port ở `external/notification/sms/` ready<br>• Production v1: KHÔNG enable SMS (Email + Push đủ OTP + booking confirm)<br>• v1.x activate khi đo được Email OTP fail rate justify SMS backup: candidate **eSMS.vn** (VN local, ~600-1500 VND/SMS + Brandname MIC registration 2-3 tuần)<br>• Twilio loại do $0.05-0.07/SMS đắt + roaming VN<br><br>**C. Push = Expo Push Service** (native ADR-014, free always).<br>• Stack client: `expo-notifications` SDK trong Passenger Mobile + Employee Mobile<br>• Stack server: `expo-server-sdk-node` ở `external/notification/push/expo/`<br>• 1 endpoint `https://exp.host/--/api/v2/push/send` route qua FCM (Android) + APNs (iOS)<br>• FCM credentials (Android prod) + APNs cert (iOS prod) config trong Expo project khi EAS Build<br>• Push token persistent: `device_token` table per user device, soft-delete khi logout<br>• Use case: Passenger (booking confirmed, departure reminder, refund processed, dispute update); Employee (trip schedule, passenger boarding, support ticket)<br>• Migration path: nếu cần FCM analytics/A-B sau → adapter swap FCM Direct, business code không đổi<br><br>**D. OAuth = Google + Facebook + Apple v1** (Passenger only per ADR-017; Zalo defer v1.x).<br>• Stack: Better Auth built-in OAuth plugins (`googleProvider`, `facebookProvider`, `appleProvider`) — config 1 dòng per provider<br>• **Apple Sign-In mandatory** vì App Store Guideline 4.8 — iOS app có Google/FB → bắt buộc Apple Sign-In ngang hàng<br>• Apple Developer Program $99/yr **= sunk cost ADR-014** (EAS Submit iOS đã cần) → Apple OAuth no marginal cost v1<br>• Account linking: Better Auth tự merge nếu OAuth email match Passenger account (email-OTP) đã có<br>• Operator/Platform: KHÔNG enable OAuth (closed enrollment `{scope}/{username}` per ADR-017)<br>• **Zalo defer v1.x**: cần custom Better Auth adapter (không có built-in) + Zalo Official Account + KYC business 2-3 tuần; audience overlap Facebook ~70-80%; ROI thấp v1; re-evaluate post-MVP traction<br><br>**Fan-out architecture**: business event (booking confirm, payment success, payout, KYC, dispute) → enqueue BullMQ Layer 9 job `notify-fan-out` → worker dispatch song song 3 adapter (email + push + sms-noop) → mỗi adapter ghi `notification_log` Mongo audit + retry exponential + DLQ. Mỗi user có `notification_preference` (email/push opt-in per category).<br><br>**Khanh quyết định**: chốt qua tiêu chí **"miễn phí cho v1"** áp dụng toàn bộ 4 sub-decision → AI áp recommendation tier free/no-marginal-cost: Q1=A (Resend), Q2=α (defer SMS), Q3=A (Expo Push), Q4=A (Google+Facebook+Apple + Zalo defer). **CRITICAL re-confirm Sprint 5-6** khi rework 07 Security §5/§6 + LLD notification chi tiết + có data volume Email/Push thực tế (Resend free 3k cap, Expo Push fair-use). |
| Alternatives đã xét | **A. Email**: SendGrid (mature deliverability, alt nếu Resend deliverability VN gặp issue), Postmark ($15/mo paid, best transactional reputation), Brevo (free 300/day, alt), AWS SES (rẻ nhất $0.10/1k nhưng DX yếu), SMTP self-host (loại — IP reputation khó solo+AI). **B. SMS**: Hướng β chốt sớm — eSMS.vn / VietGuys / Stringee / FPT-Viettel enterprise. Twilio loại do cost + roaming. **C. Push**: FCM Direct (vẫn fit Expo bridge nhưng cần Firebase project + analytics nếu muốn segment); OneSignal (dashboard tốt free 10k user, alt); AWS SNS (low-level $0.50/1M); native FCM+APNs ejected (vi phạm ADR-014 không eject, loại). **D. OAuth**: Zalo (VN dominant 70M nhưng custom Better Auth adapter + Zalo OA KYC nặng → defer); Microsoft/GitHub/Discord/X (low VN reach, loại); chỉ Email+OTP no OAuth (UX kém + conversion thấp); **phương án VN-first** Zalo+Google+Apple skip Facebook (defer cho khi có Zalo OA approval). |
| Hệ quả tích cực | (a) **Cost v1 ≈ $0** — Resend free tier + SMS defer + Expo Push free + OAuth free. Apple Dev Program $99/yr là sunk cost ADR-014 không thêm chi phí marginal. (b) **DX hiện đại** — Resend + React Email + `expo-notifications` + Better Auth OAuth built-in: tất cả TS-first, AI assist top, setup vài giờ. (c) **Adapter ready cho mở rộng** — SMS port LocalLogger dev sẵn, kích hoạt v1.x dễ; Zalo OAuth thêm sau qua custom adapter Better Auth. (d) **Fan-out async via BullMQ** (Layer 9) — không nghẽn booking flow, retry/DLQ free, observability `/admin/queues` Bull Board. (e) **Account linking auto** Better Auth — OAuth email match Email-OTP account → merge → không phá identity model ADR-017. (f) **Permission isolation Mobile** — Push chỉ cần `notifications` permission đơn giản; ADR-014 2-app split (Passenger minimal + Employee full) tương thích. (g) **Strangler-ready** — đổi Email/Push/OAuth provider sau qua adapter; SMS activate sau qua adapter. (h) **Compliance đơn giản v1** — không vướng SMS Brandname MIC (vì defer); Email PII cross-border NĐ 13/2023 standard practice (Resend US HQ, disclose Privacy Policy). |
| Hệ quả tiêu cực & mitigation | (1) **Resend free 3k/mo cap** → traction tăng phải upgrade $20/mo hoặc swap → mitigate: monitor Email volume sớm; adapter abstraction cho swap SendGrid/AWS SES nhanh; template React Email export portable HTML. (2) **Email deliverability VN** (Resend cần warm IP + DKIM/SPF) → spam folder risk OTP critical → mitigate: setup DNS records cẩn thận; monitor bounce rate qua Resend webhook; fallback alternate provider qua adapter nếu fail rate cao; ngắn hạn không có SMS backup. (3) **Không SMS v1** — Email OTP delivery rate < 95% → user mới không vào được → mitigate: OTP resend cooldown rate-limit (SEC-OQ-07); magic-link fallback qua email; v1.x activate SMS khi đo được fail rate justify. (4) **Expo Push fair-use** (free, Expo có thể throttle abuse) → mitigate: monitor send rate; production volume v1 nhỏ; có thể migrate FCM Direct qua adapter sau. (5) **Apple Sign-In cert setup** (cần Apple Developer paid account $99/yr — đã sunk ADR-014, cert config phức tạp) → mitigate: cert config 1 lần khi EAS Submit iOS Sprint 4 DevOps; doc trong LLD Sprint 5-6. (6) **OAuth account linking edge case** (OAuth email khác Email-OTP account email → 2 accounts riêng) → mitigate: Better Auth UI cho user link manual sau login; document trong Privacy Policy. (7) **Zalo defer = có thể mất audience VN-only** → mitigate: track conversion rate Facebook vs no-Zalo post-MVP; activate Zalo nếu metric demand justify cost Zalo OA registration. (8) **CRITICAL re-confirm Sprint 5-6** do Khanh chốt qua tiêu chí "miễn phí" (không direct per-Q selection) → tương tự ADR-017 + ADR-019 pattern → flag re-confirm khi rework 07 Security §5/§6 (OTP delivery flow, OAuth account linking policy) + LLD notification chi tiết Sprint 5-6 + khi có data volume thực tế Email/Push. |
| Nguồn | Sprint 3 Layer 13 workshop (31/05/2026), Phase 3 VN vendor; 4 sub-decision matrix (Email 7 option × 5 axes / SMS 5 option × 5 axes + 2 hướng α-defer/β-chốt-sớm / Push 5 option × 5 axes / OAuth 5 provider × 4 axes) + primer (SPF/DKIM/DMARC, Brandname MIC, FCM/APNs/Expo Push routing, OAuth PKCE + account linking + App Store Guideline 4.8) + compliance flag (Resend cross-border PII NĐ 13/2023 standard practice, SMS Brandname MIC defer-ready, Push token PII auth_session standard, OAuth privacy disclosure). Khanh **chốt qua tiêu chí "miễn phí cho v1"** → AI áp recommendation: Q1=A Resend (free 3k/mo), Q2=α defer SMS (free port-only), Q3=A Expo Push (always free, native ADR-014), Q4=A Google+Facebook+Apple v1 + Zalo defer v1.x (OAuth free, Apple Dev sunk cost ADR-014). **Re-close OQ-09** với wording cập nhật (Email Resend primary + SMS adapter defer + Expo Push + OAuth Google/Facebook/Apple Passenger-only, không phải chỉ "Email OTP + SMS adapter future" closure cũ 11/05/2026). Consistent **ADR-006** (notification adapter boundary), **ADR-014** (Expo Push native không eject), **ADR-017** (Better Auth OAuth Passenger-only + closed enrollment Operator/Platform), **ADR-011** (notification_log Mongo audit), **ADR-016** (BullMQ fan-out async). **Sprint 3 2/4 layer chốt — còn Layer 14 Routing, 15 Bank payout (re-close OQ-16).** |

---

### ADR-021. Routing Engine + Map/Geocoding v1: Mapbox (Matrix API + GL) free tier + OSRM self-host escape-hatch

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (31/05/2026 — Sprint 3 Layer 14 workshop output, Phase 3 VN vendor) |
| Bối cảnh | Layer 14 quyết routing/geo cho marketplace. **Strategic reframe quan trọng**: đây là marketplace vé xe khách **tuyến cố định**, KHÔNG phải ride-hailing. Route do Operator định nghĩa sẵn từ catalog stop-point chuẩn Platform (FR-OPS-04, DP-08, AS-15); tọa độ chuẩn hóa sẵn. → Routing là dependency mức **"assist"** chỉ phục vụ: (1) tính khoảng cách + thời gian giữa stop-point **lúc config route/trip** (DP-07, dòng 428) → tính 1 lần, **cache DB vĩnh viễn**; (2) gợi ý điểm đón/trả gần (geo proximity); (3) hiển thị bản đồ marker stop-point ở UI. **Ngoài phạm vi v1**: tối ưu lộ trình AI realtime (SRS dòng 346), IoT GPS/OBD hardware (350), turn-by-turn navigation. → Volume routing API **cực thấp** (chỉ lúc config, không phải mỗi search/booking) → free tier hosted dư sức → **self-host OSRM 24/7 là over-engineering cho v1**. **2 sub-decision tách biệt**: A. Routing/Distance-Matrix engine (backend `external/routing/`); B. Map tiles + Geocoding (frontend display + Admin/Operator pick tọa độ stop-point mới). Adapter port mandated (ADR-006, NFR-COMP-05). **Data residency N/A**: tọa độ stop-point = địa điểm công cộng (bến xe) KHÔNG phải PII → gửi API nước ngoài không vướng Nghị định 13/2023 (khác KYC OQ-21). **Carryover trap flag**: OSRM là default legacy (DP-07 + DOMAIN-MAP `external/routing/osrm/`) — đánh giá lại fresh, không rubber-stamp vì legacy. Khanh giữ tiêu chí "miễn phí cho v1". Workshop 2 sub-decision matrix + primer + carryover flag. |
| Quyết định | **A. Routing/Matrix = Mapbox Matrix/Directions API** (free tier 100k Matrix req/mo).<br>• Stack: HTTP client + Mapbox token, code trong `external/routing/mapbox/` sau `RoutingProvider` port (interface)<br>• Distance + duration giữa stop-point tính lúc Operator config route/trip → **cache vào DB** (`route_stop` distance/duration field hoặc bảng `route_segment_metric`) → API call 1 lần, không lặp<br>• Gợi ý điểm đón/trả: geo proximity query (PostGIS trên Postgres ADR-011 hoặc Mapbox nearby) — defer chi tiết LLD<br><br>**B. Map + Geocoding = Mapbox GL** (free tier 50k map loads/mo).<br>• Web (Layer 6): `mapbox-gl` / `react-map-gl`<br>• Mobile (Layer 7 Expo): `@rnmapbox/maps` (Expo config plugin)<br>• Geocoding: forward (Admin nhập địa chỉ stop-point → tọa độ) + reverse (pick trên map → địa chỉ) — volume admin-side thấp<br>• **Cùng access token Sub A** → 1 vendor, 1 SDK, 1 API key<br><br>**OSRM self-host = documented escape-hatch** (Q3): KHÔNG build v1; DOMAIN-MAP `external/routing/osrm/` chừa sẵn chỗ; swap `RoutingProvider` adapter sang OSRM khi (a) volume vượt free tier đáng kể, hoặc (b) cần data residency tuyệt đối / unlimited request sau v1.x.<br><br>**Adapter swap**: `RoutingProvider` port (ADR-006) — Mapbox-specific code chỉ trong `external/routing/mapbox/`; swap OSRM/Google/ORS/Leaflet sau không phá business code.<br><br>**Khanh quyết định**: chốt A + B **theo khuyến nghị** (giữ tiêu chí "miễn phí cho v1") → Q1=A Mapbox Matrix, Q2=A Mapbox GL, Q3=A OSRM escape-hatch documented. |
| Alternatives đã xét | **A. Routing**: (1) **OpenRouteService (ORS)** hosted free 2000 req/day (rate-limited) — fallback free tốt nhưng quota chặt nếu spike; cũng self-host được. (2) **Google Directions/Distance Matrix** — VN coverage tốt nhất, $200/mo credit nhưng cần thẻ + billing account upfront. (3) **OSRM self-host** — free license + unlimited + data residency, nhưng **ops burden cao** (tải OSM Vietnam extract, pipeline `osrm-extract/contract`, VPS 24/7, maintenance) mâu thuẫn Khanh "nghiệp dư infra" + tinh thần "miễn phí" (thời gian ops = chi phí thật). Giữ làm **escape-hatch**. (4) **GraphHopper** self-host/hosted — tương tự OSRM, defer. (5) **Valhalla** self-host — ops cao nhất, loại v1. **B. Map+Geocoding**: (1) **Leaflet + OSM raster tiles** — 100% free no account (OSM fair-use), zero-vendor; đánh đổi UI thô + ghép Nominatim geocoding riêng. Alt nếu Khanh muốn tránh hẳn vendor. (2) **Google Maps JS + Places** — chất lượng nhất, $200 credit cần thẻ. (3) **MapTiler** — free 100k tiles/mo, alt. **Tiêu chí "miễn phí"**: Mapbox thắng vì free tier rộng + zero-ops (OSRM "free license" nhưng tốn ops thật; Google cần thẻ; ORS quota chặt). |
| Hệ quả tích cực | (a) **Cost v1 = $0** — Mapbox free 100k Matrix + 50k map loads/mo, volume thật vài nghìn (cache sau config) → dư xa. (b) **Zero-ops** — không VPS, không pipeline OSM data, không maintenance; hợp Khanh nghiệp dư infra (Layer 8 self-rate). (c) **1 vendor cho cả routing + map + geocoding** — 1 access token, 1 SDK pattern Web + Mobile (`@rnmapbox/maps` Expo). (d) **Cache-once pattern** — distance/duration tính lúc config → lưu DB → search/booking đọc DB không gọi API → vừa nhanh vừa tiết kiệm quota. (e) **Adapter (ADR-006)** — swap OSRM/Google/ORS/Leaflet sau không phá business code; Strangler-ready. (f) **Tránh carryover trap** — không rubber-stamp OSRM legacy; chọn theo volume thật + ops reality. (g) **Data residency không vướng** — tọa độ stop-point không PII → API nước ngoài OK, không cần DPIA (khác KYC). (h) **OSRM escape-hatch sẵn sàng** — nếu sau cần unlimited/residency, DOMAIN-MAP đã chừa chỗ + adapter rẻ để swap. |
| Hệ quả tiêu cực & mitigation | (1) **Mapbox free tier cap** (100k Matrix / 50k map loads/mo) → traction tăng có thể vượt → mitigate: cache distance/duration DB giảm API call tận gốc; monitor usage Mapbox dashboard; swap adapter OSRM self-host / Google nếu vượt. (2) **Vendor lock Mapbox** → mitigate: `RoutingProvider` port abstraction, code Mapbox chỉ trong `external/routing/mapbox/`; map tiles abstraction ở `packages/ui`; swap rẻ. (3) **OSRM escape-hatch chưa build** → nếu cần gấp residency/volume → mitigate: DOMAIN-MAP `external/routing/osrm/` chừa sẵn; adapter pattern khiến build OSRM sau là isolated; document pipeline trong LLD khi cần. (4) **Mapbox cần access token + account** (không hẳn zero-account) → mitigate: free tier không cần thẻ ban đầu; token quản qua env config (consistent secret management). (5) **Map quality VN** (Mapbox OSM-based, có thể thiếu chi tiết vùng sâu) → mitigate: stop-point từ catalog chuẩn Platform đã có tọa độ chính xác (DP-08); geocoding chỉ assist Admin; cho phép manual pick-on-map override. (6) **Mobile `@rnmapbox/maps`** cần config native qua EAS Build (Expo config plugin) → mitigate: setup 1 lần khi Sprint 4 DevOps; document LLD Sprint 5-6. (7) **Distance/duration cache stale** nếu đường thực tế thay đổi → mitigate: v1 tuyến ít đổi, acceptable; cache refresh periodic/manual khi Operator cập nhật route; không ảnh hưởng money (fare riêng per OQ-08). **Re-confirm nhẹ** khi LLD Sprint 5-6 nếu volume vượt free tier — KHÔNG gắn CRITICAL như ADR-017/019/020 vì routing low-risk assist-level + adapter-swappable + cache-once. |
| Nguồn | Sprint 3 Layer 14 workshop (31/05/2026), Phase 3 VN vendor; 2 sub-decision matrix (Routing 6 option Mapbox/ORS/Google/OSRM-self-host/GraphHopper/Valhalla × 6 axes [VN coverage / free tier / ops burden / DX-AI assist / NestJS-adapter fit / time-to-v1]; Map+Geocoding 4 option Mapbox GL/Leaflet+OSM/Google/MapTiler) + primer (routing-matrix vs map-tiles-geocoding tách biệt, self-host vs hosted ops trade-off, cache-once pattern) + carryover trap flag (OSRM legacy DP-07 + DOMAIN-MAP). Khanh chốt A + B **theo khuyến nghị** (tiêu chí "miễn phí cho v1") → Q1=Mapbox Matrix API, Q2=Mapbox GL, Q3=OSRM escape-hatch documented. **Không re-close OQ** (OQ-16 bank payout là Layer 15). **Data residency N/A** (tọa độ stop-point không PII) → không raise OQ mới. Consistent **ADR-006** (routing adapter boundary), **ADR-011** (cache distance/duration Postgres + PostGIS proximity), **DP-07/DP-08, AS-15, FR-OPS-04/06, FR-ADM-15, NFR-COMP-05**. Supersede ngầm lựa chọn legacy OSRM-default (DP-07 ghi "OSRM hoặc tương đương") → Mapbox primary, OSRM escape-hatch. **Sprint 3 3/4 layer chốt — còn Layer 15 Bank payout (re-close OQ-16) là layer đóng Sprint 3.** |

---

### ADR-022. Bank Payout Channel v1: Manual admin-confirm + batch export sau `PayoutProvider` adapter; auto-disbursement defer v1.x

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (01/06/2026 — Sprint 3 Layer 15 workshop output, Sprint 3 closing layer, Phase 3 VN vendor) |
| Bối cảnh | Layer 15 đóng Sprint 3 — quyết kênh payout (vế **CHI** escrow, đối xứng vế THU Layer 12). Flow: chuyến COMPLETED → escrow của Operator thành "đủ điều kiện" → cron T+3 gom thành `Payout` batch → chuyển tiền tới `BankAccount` Operator → ghi `ReconciliationRecord`. **SRS đã định hướng rõ**: OQ-16 closure (dòng 328) = "T+3, không ngưỡng tối thiểu v1, **chuyển khoản ngân hàng + Admin xác nhận thủ công**"; dòng 430 = kênh payout "chuyển khoản trực tiếp hoặc **qua bên thứ ba khi mở rộng**". **Ràng buộc đã khóa**: `Payout` gom khoản đủ điều kiện − commission − refund − adjustment − holdback (dòng 570); tiền vào `BankAccount` verified từ KYC (OQ-19, dòng 545); `ReconciliationRecord` + reconciliation job (DP-18); thao tác tiền thủ công log who/why/when (CO-27); payout policy versioning (AS-23); commission 5% (OQ-18); BullMQ cron T+3 concurrency 1 (ADR-016); adapter mandated (ADR-006, NFR-COMP-05). **Liên hệ OQ-22 (giấy phép TGTT)**: payout = "chi cho Operator" đã nằm trong OQ-22 (Layer 12, escrow giữ + chi = hoạt động TGTT theo NĐ 52/2024) → KHÔNG raise OQ mới; auto-disbursement production phụ thuộc OQ-22. Khanh giữ tiêu chí "miễn phí cho v1". Workshop 4 option matrix + primer + liên hệ OQ-22. |
| Quyết định | **v1 = Manual admin-confirm + batch export** sau `PayoutProvider` port (`external/payout/`), implementation v1 = `ManualPayoutAdapter`.<br><br>**Flow chi tiết**:<br>• **BullMQ cron T+3** (concurrency 1, ADR-016) chạy chu kỳ gom escrow đủ điều kiện của mỗi Operator → tạo `Payout` trạng thái PENDING<br>• Số tiền payout = escrow eligible **− commission (OQ-18, snapshot rate) − refund − adjustment − holdback** (dòng 570)<br>• Admin review batch trong Platform admin panel → **tự chuyển khoản** qua internet/corporate banking tới `BankAccount` đã verify (KYC OQ-19)<br>• Admin **nhập mã giao dịch ngân hàng** (bank reference) → hệ thống set `Payout` COMPLETED + tạo `ReconciliationRecord`<br>• **Batch export** (CSV/file) cho admin đối chiếu hoặc import bulk-transfer nếu ngân hàng hỗ trợ<br>• Mọi thao tác audit who/why/when (CO-27); payout policy versioned (AS-23); T+3 không ngưỡng tối thiểu (OQ-16)<br>• Operator xem escrow balance + lịch sử payout (FR-OPR-07) để đối chiếu<br><br>**v1.x auto-disbursement (Q2, roadmap)**: **VNPay/MoMo chi hộ API** ưu tiên (reuse adapter family Layer 12 ADR-019) — chỉ thêm `VnpayDisbursementAdapter`/`MomoDisbursementAdapter` sau `PayoutProvider` port. Kích hoạt khi **OQ-22 (giấy phép TGTT) được giải quyết** — auto chi hộ qua cổng licensed chính là **hướng (b) của OQ-22**. Bank direct API + third-party disbursement (PayOS payout/napas) = alternative defer.<br><br>**Maker-checker (Q3, documented mitigation)**: manual payout = admin có quyền chi tiền → v1 chỉ Khanh (super-admin) chấp nhận được; khi team Platform >1 người → bật **dual-control**: tách `PlatformAdmin` (maker tạo/duyệt batch) vs checker (xác nhận đã chuyển) qua RBAC (ADR-017). KHÔNG build v1, chỉ document.<br><br>**Khanh quyết định**: Q1 = Manual admin-confirm + batch export (chốt trực tiếp); Q2 + Q3 giữ tiêu chí "miễn phí cho v1" → AI áp recommendation (VNPay/MoMo chi hộ roadmap + maker-checker documented). Re-confirm nhẹ khi LLD nếu auto-disbursement bật (tied OQ-22) — KHÔNG CRITICAL vì Q1 chốt trực tiếp khớp SRS OQ-16 + low-risk. |
| Alternatives đã xét | (1) **Auto VNPay/MoMo chi hộ ngay v1** — tự động hóa payout, reuse adapter Layer 12. Yếu: có phí/giao dịch (mâu thuẫn "miễn phí v1"); cần hợp đồng chi hộ + thường cần giấy phép (OQ-22 chưa giải quyết); sandbox/MVP không cần auto. Defer v1.x. (2) **Bank direct / corporate banking API** (VCB/BIDV/Techcombank/MB...) — chuyển khoản tự động trực tiếp. Yếu: onboarding nặng, tích hợp per-bank khác nhau, hợp đồng doanh nghiệp; không hợp solo+AI MVP. Defer. (3) **Third-party disbursement** (PayOS payout / napas bulk / fintech aggregator) — chuyên bulk payout. Yếu: phí + cần license/hợp đồng; "bên thứ ba khi mở rộng" (dòng 430) = đúng tinh thần nhưng defer v1.x. (4) **Manual admin-confirm + batch export** (chọn) — zero tích hợp, $0, đúng OQ-16 closure, chạy được sandbox/MVP không cần license, đủ cho scale MVP (ít Operator). |
| Hệ quả tích cực | (a) **Cost v1 = $0** — không phí tích hợp, không phí/giao dịch chi hộ → đúng tiêu chí "miễn phí". (b) **Khớp SRS OQ-16 closure** (dòng 328) — không phải quyết định mới, chỉ formalize + thêm adapter. (c) **Chạy được sandbox/MVP** — không cần giấy phép TGTT (OQ-22) để demo/test; auto chi hộ mới cần. (d) **`PayoutProvider` adapter ready** — v1.x thêm auto-disbursement (VNPay/MoMo chi hộ) không phá business code; Strangler-ready. (e) **Reuse BullMQ Layer 9** — batch computation cron T+3 concurrency 1 (ADR-016) đã dự trù; reconciliation job (DP-18) cùng cơ chế. (f) **Audit trail đầy đủ** — CO-27 log who/why/when + `ReconciliationRecord` + `Payout` status history → minh bạch tài chính (mục tiêu giữ chân Operator, dòng 231). (g) **An toàn tiền v1** — admin review từng batch trước chuyển, không auto-chi nhầm; maker-checker path sẵn khi team lớn. (h) **Đối xứng adapter Layer 12** — vế THU (PaymentGateway) + vế CHI (PayoutProvider) cùng pattern, mental model nhất quán. |
| Hệ quả tiêu cực & mitigation | (1) **Manual không scale** (nhiều Operator → admin labor T+3 tăng) → mitigate: batch export bulk-transfer file (import internet banking nếu bank hỗ trợ); swap adapter auto-disbursement v1.x khi volume Operator tăng. (2) **Manual payout = rủi ro fraud/nhầm với 1 admin** → mitigate: **maker-checker dual-control documented** (RBAC PlatformAdmin maker vs checker, ADR-017); audit CO-27; v1 chỉ Khanh super-admin chấp nhận được; bật dual-control khi team >1. (3) **Real-money payout phụ thuộc OQ-22 escrow legality** → mitigate: sandbox/MVP không vướng; production tied OQ-22; auto chi hộ qua cổng licensed (VNPay/MoMo) là hướng (b) OQ-22 → giải quyết cùng lúc. (4) **Mã giao dịch ngân hàng nhập tay sai** → mitigate: validation format + reconciliation job đối chiếu (DP-18) + Operator xem payout history confirm (FR-OPR-07). (5) **`BankAccount` KYC sai/Operator đổi tài khoản** → mitigate: bank info change cần re-verify + admin approve + audit; payout chỉ tới verified account; snapshot account info tại thời điểm payout. (6) **T+3 cron lỗi/chạy trùng → double payout** → mitigate: BullMQ concurrency 1 (ADR-016) + idempotent `Payout` per `(operatorId, period)` unique + status guard (chỉ PENDING → COMPLETED). (7) **Payout/commission policy đổi áp ngược vé cũ** → mitigate: AS-23 versioning + snapshot commission rate tại thời điểm booking/payout (DM-04). Re-confirm nhẹ khi LLD nếu auto-disbursement bật — KHÔNG CRITICAL như ADR-017/019/020 vì Q1 chốt trực tiếp + khớp SRS + low-risk. |
| Nguồn | Sprint 3 Layer 15 workshop (01/06/2026), Sprint 3 closing layer, Phase 3 VN vendor; matrix 4 option (Manual admin-confirm / Auto VNPay-MoMo chi hộ / Bank direct API / Third-party disbursement) × axes (tích hợp / phí / MVP fit / phụ thuộc OQ-22 / khớp SRS) + primer (payout flow escrow eligible − commission − refund − adjustment − holdback dòng 570; 3 cơ chế chuyển tiền; liên hệ OQ-22). Khanh chốt **Q1 = Manual admin-confirm + batch export** + giữ tiêu chí "miễn phí cho v1" cho Q2 (auto-disbursement roadmap VNPay/MoMo chi hộ) + Q3 (maker-checker documented). **Re-close OQ-16** với wording cập nhật (Manual admin-confirm + batch export sau `PayoutProvider` adapter + auto-disbursement defer v1.x, giữ tinh thần closure cũ 11/05/2026 + thêm adapter abstraction). **KHÔNG raise OQ mới** (OQ-22 cover payout legal "chi cho Operator"). Consistent **ADR-005** (escrow → payout T+N), **ADR-006** (payout adapter boundary), **ADR-016** (BullMQ cron T+3 concurrency 1), **ADR-019** (payment gateway family — chi hộ reuse v1.x), **ADR-017** (RBAC maker-checker), **OQ-18** (commission), **OQ-19** (KYC BankAccount), **OQ-22** (TGTT license), **AS-17/AS-23, CO-10/CO-27, DM-03/DM-04, DP-09/DP-11/DP-18, FR-OPR-07, FR-ADM-10**. **Sprint 3 CLOSED — 4/4 layer Phase 3 VN vendor (Payment / Notification+OAuth / Routing / Payout). Toàn bộ 5 OQ vendor re-opened (OQ-05/09/10/14/16) đã re-closed — KHÔNG còn OQ vendor block MVP. Phase 4 DevOps (Sprint 4) mở khóa.** |

---

### ADR-023. Deploy target v1: Render managed PaaS (Singapore) cho Node API + BullMQ worker; DB managed-separate (Postgres Supabase/Neon + Mongo Atlas SG)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (01/06/2026 — Sprint 4 Layer 16 workshop output, Phase 4 DevOps opening layer) |
| Bối cảnh | Layer 16 mở Sprint 4 Phase 4 DevOps (chạy sau Sprint 5 rework 5 doc batch — Khanh chọn hướng (b) rework trước rồi DevOps). Cần deploy target production cho stack đã chốt 15 layer. **Reframe quan trọng**: phần lớn stack đã managed → deploy target chỉ thực sự cần host process tự viết: Redis = Upstash SG (ADR-015), R2 = Cloudflare (ADR-018), SaaS = Mapbox/Resend/Expo/VNPay/MoMo; DB managed-separate per ADR-011 (Postgres Supabase/Neon SG + Mongo Atlas SG). → quyết định Layer 16 = host cho **Node API + BullMQ worker** (+ 3 Next.js frontend, chi tiết defer LLD FE). Khanh self-rate nghiệp dư ops + tiêu chí "miễn phí v1". Workshop 4 option matrix + VN cloud defer + primer (container/PaaS/VPS). |
| Quyết định | v1 = **Render managed PaaS** region **Singapore**.<br><br>**Mapping service**:<br>• API = Render **Web Service** (Docker image, Node 22)<br>• BullMQ worker = Render **Background Worker** service (process tách — resolve worker deployment defer ADR-016; chi tiết Layer 17 ADR-024)<br>• Payout T+3 + cron = Render **Cron Job** service hoặc BullMQ `repeat` trong worker (chốt Layer 17)<br>• Postgres 16 = **managed-separate** Supabase / Neon SG (ADR-011)<br>• Mongo 7 audit = **managed-separate** Atlas SG (ADR-011)<br>• Redis = Upstash SG (ADR-015); R2 = Cloudflare (ADR-018)<br>• Frontend (marketplace/operator-os/admin) = Render Web Service (Next.js SSR Docker) hoặc Vercel — chi tiết defer LLD FE<br><br>**Nguyên tắc**: **Docker image** (portable, không lock Render — swap Fly/VPS dễ); **GitHub auto-deploy** built-in (tie Layer 19 CI/CD); secret qua Render env group; API service paid tier always-on (tránh free-tier cold start). |
| Alternatives đã xét | (1) **Railway** — PaaS DX đơn giản nhất (push-to-deploy), SG region, Docker. Yếu: worker + cron phải cấu hình thủ công (không có service type chuyên như Render). Lựa chọn tốt thứ 2. (2) **Fly.io** — Docker-native + edge, region SG (`sin`), multi-process qua `fly.toml`, kiểm soát nhiều hơn. Yếu: config nhiều hơn PaaS, learning curve cao hơn cho nghiệp dư. Escape-hatch nếu cần control. (3) **VPS + Docker Compose** (Hetzner/DO/Vultr SG) — rẻ nhất (~$6-12 all-in), full control, self-host được cả Postgres+Mongo. Yếu: ops nặng nhất (tự lo OS/SSL/backup/scaling/security) — không hợp solo nghiệp dư v1. (4) **VN cloud** (Viettel IDC / VNG Cloud / FPT Cloud) — data residency KYC/payment (OQ-21/22), VND billing, VN support. Yếu: DX kém PaaS, setup nặng, Docker support không đồng đều. **Defer** tied OQ-21/22 production prep — KHÔNG hợp MVP solo. |
| Hệ quả tích cực | (a) **Worker + Cron first-class** — Render có sẵn 3 service type Web + Background Worker + Cron Job khớp thẳng BullMQ worker (Layer 17) + payout T+3 cron (ADR-016/022) không cấu hình thủ công. (b) **Ops thấp** hợp Khanh nghiệp dư — Render lo OS/SSL/restart/scaling. (c) **Cost "miễn phí v1"** — free tier + ~$7/service paid, không phí setup. (d) **SG region** ~30-50ms từ VN, đồng bộ Upstash/Atlas/Supabase SG. (e) **Docker portable** — không lock; swap Fly/VPS/VN-cloud sau bằng cùng image. (f) **DB managed-separate** — Postgres Supabase/Neon + Mongo Atlas tự lo backup/replication/PITR, Render chỉ chạy Node process. (g) **GitHub auto-deploy** built-in → unblock Layer 19 CI/CD phần lớn. (h) **Strangler-ready** — tách service/scale độc lập; eject sang Fly/VPS khi cần. |
| Hệ quả tiêu cực & mitigation | (1) **PaaS đặt ngoài VN** → data residency KYC/payment production → mitigate: gắn **OQ-21/22**; MVP dùng KYC dev-local (ADR-018) không vướng; khi go-live cân VN cloud/split. (2) **Free tier cold-start/sleep** → mitigate: API service dùng paid tier always-on; worker/cron paid. (3) **Vendor lock Render config** (render.yaml) → mitigate: **Docker image + env standard** → swap Fly/VPS bằng cùng image; render.yaml mỏng. (4) **Render Postgres addon vs managed-separate** → mitigate: chọn Supabase/Neon (backup/PITR/branching tốt hơn addon) per ADR-011; Render chỉ host app. (5) **Cost tăng theo số service** (Web + Worker + Cron) → mitigate: monitor; gộp worker+cron nếu volume thấp; in-process fallback Layer 17 nếu cần. (6) **Region SG không phải VN** → latency ~30-50ms acceptable (đồng bộ các managed service SG). |
| Nguồn | Sprint 4 Layer 16 workshop (01/06/2026), Phase 4 DevOps opening layer; matrix 4 option (Render / Railway / Fly.io / VPS Docker Compose) + VN cloud defer × axes (ops nghiệp dư / cost / worker+cron fit / region VN / Docker / DB managed / CI-CD / scaling / time-to-v1) + primer (container/PaaS/VPS) + reframe stack-đã-managed. Khanh chốt **"Render (Recommended)"** + tiêu chí "miễn phí v1". **Influences Layer 17** (worker deployment = Render Background Worker — resolve defer ADR-016). **Unblock 09 Deploy** (partial — full khi Layer 17-19 chốt). Consistent **ADR-002** (modular monolith 1 API process + worker tách), **ADR-011** (DB managed Supabase/Atlas SG), **ADR-015** (Redis Upstash SG), **ADR-016** (BullMQ worker + cron), **ADR-018** (R2). Tied **OQ-21/22** (data residency production). Phase 4 còn 3 layer: Worker deployment (17) / Test framework (18) / Monitoring+CI-CD (19). |

---

### ADR-024. Worker deployment v1: BullMQ worker tách thành Render Background Worker service riêng (resolve defer ADR-016)

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (01/06/2026 — Sprint 4 Layer 17 workshop output, Phase 4 DevOps) |
| Bối cảnh | Layer 17 resolve **worker deployment defer từ ADR-016** ("v1 tạm in-process cùng API, refactor separate worker process khi container/deploy target chốt"). Render chốt Layer 16 (ADR-023) với native Background Worker service type → đúng trigger refactor. BullMQ jobs (ADR-016): booking notification fan-out, audit batch → Mongo, payout T+3 cron, webhook retry + DLQ, reconciliation querydr, reporting aggregation. Câu hỏi: worker chạy tách process hay in-process với API. |
| Quyết định | **Tách BullMQ worker thành Render Background Worker service riêng**, cùng Docker image với API, khác start command.<br><br>• Codebase: `apps/api/src/main.ts` (API HTTP bootstrap) + `apps/api/src/worker.ts` (BullMQ worker bootstrap) — cùng monorepo, share module/DI (per ADR-002/016 prep).<br>• Render: **Web Service** (API, paid always-on) + **Background Worker** (worker, always-on) deploy từ **cùng image cùng commit** (1 GitHub push → Render build 1 image → chạy 2 service khác start command).<br>• **Payout T+3 cron** = BullMQ **repeatable job** `repeat: { pattern: '0 0 * * *' }` concurrency 1 trong worker (giữ ADR-016; KHÔNG dùng Render Cron Job riêng vì worker đã always-on).<br>• Worker + API share connection Redis Upstash (ADR-015) + Prisma/Postgres + Mongoose/Mongo (audit).<br>• Resolve ADR-016 worker deployment defer. |
| Alternatives đã xét | (1) **In-process** (worker trong API process) — rẻ hơn 1 service, đơn giản nhất; yếu: heavy job (payout aggregate, reporting) block API event loop + không scale worker riêng. ADR-016 đánh dấu stopgap tạm v1. (2) **Render Cron Job service riêng** cho payout (thay BullMQ repeat) — Render spin container theo schedule; yếu: worker đã always-on nên BullMQ repeat đơn giản + nhất quán hơn; Render Cron Job thừa. (3) **Worker repo/codebase riêng** — over-engineer, mất share module/type; cùng image khác start command đủ + Strangler-ready tách sau nếu cần. |
| Hệ quả tích cực | (a) **Heavy/sensitive job off API event loop** — payout T+3 + reconciliation + reporting không tranh event loop với request hành khách. (b) **Scale worker độc lập** — tăng worker instance không đụng API. (c) **Render Background Worker native** — cùng image khác start command, setup tối thiểu (lý do chọn Render Layer 16). (d) **Resolve ADR-016 defer sạch** trước khi ship code mới, không refactor in-process→separate sau. (e) **Share code/connection** — cùng monorepo, reuse module/DI/Prisma/ioredis. (f) **Cron giữ ADR-016** — BullMQ repeatable concurrency 1, không thêm cơ chế. (g) **Strangler-ready** — tách worker repo/scale riêng post-v1 dễ. |
| Hệ quả tiêu cực & mitigation | (1) **+1 service cost** (~$7/mo) → acceptable cho an toàn event loop; monitor; fallback in-process nếu volume cực thấp. (2) **2 process cùng codebase** init nhầm (HTTP server start trong worker) → mitigate: tách `main.ts`/`worker.ts` rõ + chỉ bootstrap module cần thiết per process. (3) **Worker crash → job pending** → mitigate: BullMQ persist Redis AOF (Upstash) + Render auto-restart + stalled job detection heartbeat (ADR-016). (4) **Cron chạy trùng nếu scale >1 worker** → mitigate: BullMQ concurrency 1 + idempotent per `(operatorId, period)` (ADR-016/022). (5) **Deploy lệch version 2 service** → mitigate: cùng image cùng commit, Render deploy cả 2 từ 1 push (CI/CD Layer 19). |
| Nguồn | Sprint 4 Layer 17 workshop (01/06/2026); **resolve worker deployment defer ADR-016**; Khanh chốt "Tách (Render Background Worker) (Recommended)". Consistent **ADR-002** (worker tách process), **ADR-016** (BullMQ + worker deployment defer — NOW resolved), **ADR-023** (Render Background Worker service type), **ADR-015** (Redis Upstash shared), **ADR-022** (payout cron concurrency 1). Phase 4 còn 2 layer: Test framework (18) / Monitoring+CI-CD (19). |

---

### ADR-025. Test framework v1: Vitest + Supertest + Playwright + Maestro

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (01/06/2026 — Sprint 4 Layer 18 workshop output, Phase 4 DevOps) |
| Bối cảnh | Layer 18 test framework cho stack TS monorepo (NestJS BE + Next FE + Expo Mobile, ADR-010/013/014). Cần cover test pyramid: unit/integration (BE+FE), e2e API, e2e web, e2e mobile. **Rủi ro cao nhất cần test**: money BIGINT/Decimal (ADR-009/011), idempotency payment/seat-hold (ADR-015/019), tenant isolation RLS (ADR-011/017), webhook HMAC dedup (ADR-019). Khanh nghiệp dư + tiêu chí "miễn phí v1" (tất cả tool free OSS). Workshop test pyramid primer + stack mapping. |
| Quyết định | **Vitest + Supertest + Playwright + Maestro**.<br><br>• **Unit + Integration** (BE NestJS + FE Next) = **Vitest** — 1 runner cho cả monorepo, ESM/Vite-native, fit Turborepo (ADR-013), AI-assist tốt.<br>• **E2E API** = **Supertest** — bắn HTTP vào NestJS app instance (không cần server riêng).<br>• **E2E Web** = **Playwright** — đa trình duyệt, codegen + trace viewer.<br>• **E2E Mobile** = **Maestro** — YAML đơn giản, fit Expo, setup nhẹ.<br>• **Integration DB** = Testcontainers (Postgres + Mongo ephemeral) hoặc docker-compose test — chốt LLD.<br>• **Mandatory test** (rủi ro cao): money math, idempotency, tenant RLS, seat-hold race, webhook HMAC dedup.<br>• **Contract test** (Pact/Dredd vs OpenAPI ADR-012) = optional, defer.<br>• Coverage target + ngưỡng chốt LLD (ưu tiên money/idempotency/tenant/security path, không ép 100%). |
| Alternatives đã xét | (1) **Jest** (unit) — default NestJS, battle-tested. Yếu: 2 runner BE/FE hoặc Jest cho cả monorepo → chậm hơn + ESM friction; Vitest API tương thích (describe/it/expect) nên swap dễ. (2) **Detox** (mobile e2e) — gray-box mạnh, đồng bộ app internals. Yếu: setup nặng (build config, sync) cho solo; Maestro đủ cho flow chính Expo. (3) **Cypress** (web e2e) — phổ biến nhưng chậm hơn Playwright, single-browser focus, ít parallel. (4) **Mocha/Chai/Jasmine** — manual setup nhiều, không tích hợp sẵn như Vitest. (5) **Jest + Detox conservative combo** — ổn định nhưng chậm + setup nặng, không tận dụng monorepo Vitest. |
| Hệ quả tích cực | (a) **1 runner Vitest** cả monorepo BE+FE → consistency + speed + Turborepo cache (ADR-013). (b) **Supertest chuẩn NestJS** — test e2e API không cần spin server. (c) **Playwright modern** — nhanh, đa trình duyệt, trace debug, AI-assist tốt. (d) **Maestro đơn giản** — YAML flow, fit Expo, learning curve thấp cho solo. (e) **Tất cả free OSS** → khớp "miễn phí v1". (f) **AI-assist tốt** cả 4 tool (training data dồi). (g) **Mandatory test rủi ro cao** định nghĩa rõ → bảo vệ money/idempotency/tenant từ đầu. |
| Hệ quả tiêu cực & mitigation | (1) **Vitest + NestJS ít battle-tested hơn Jest** → mitigate: community support solid; swap Jest dễ (cùng API describe/it/expect). (2) **Maestro ít powerful hơn Detox** → mitigate: đủ cho flow chính; swap Detox nếu cần gray-box sâu. (3) **Integration test cần DB thật** (Testcontainers) → CI chậm hơn → mitigate: tách unit (nhanh, mọi commit) vs integration (CI scheduled). (4) **E2E mobile flaky** → mitigate: Maestro retry + chạy critical path only (booking/check-in). (5) **Coverage không nên ép 100%** → mitigate: ưu tiên money/idempotency/tenant/security path; coverage gate chốt LLD. |
| Nguồn | Sprint 4 Layer 18 workshop (01/06/2026); test pyramid primer + stack mapping (unit/integration/e2e-API/e2e-web/e2e-mobile); Khanh chốt "Vitest + Supertest + Playwright + Maestro (Recommended)" + tiêu chí "miễn phí v1". Consistent **ADR-009/011** (money test), **ADR-010** (NestJS Supertest), **ADR-013** (Turborepo/Vitest), **ADR-014** (Expo/Maestro), **ADR-015/019** (idempotency/webhook test), **ADR-017** (tenant RLS test). **Unblock 08 Test Plan**. Phase 4 còn 1 layer: Monitoring+CI-CD (19). |

---

### ADR-026. Monitoring + CI/CD v1: GitHub Actions + Render auto-deploy + Sentry-centric observability

| Trường | Nội dung |
| ------ | -------- |
| Trạng thái | Proposed (01/06/2026 — Sprint 4 Layer 19 workshop output, Sprint 4 closing layer, Phase 4 DevOps) |
| Bối cảnh | Layer 19 đóng Sprint 4 Phase 4 DevOps. Cần observability (log/metric/trace/error) + CI/CD pipeline cho stack monorepo (NestJS BE + Next FE + Expo Mobile) deploy Render (ADR-023/024). Khanh nghiệp dư ops + tiêu chí "miễn phí v1". Workshop primer (log/metric/trace/error/CI-CD) + CI/CD (GitHub Actions clear) + observability 3 option matrix. |
| Quyết định | **CI/CD = GitHub Actions + Render auto-deploy; Observability = Sentry-centric.**<br><br>**CI/CD**:<br>• CI mỗi PR/push: ESLint + `tsc` typecheck + **Vitest** (ADR-025) + build + gen api-client (`openapi-typescript` ADR-012) + Turborepo affected (`turbo run --filter`).<br>• CD: merge `main` → **Render auto-deploy** (ADR-023, cùng image API + Background Worker ADR-024) + **EAS Build/Update** mobile (ADR-014) trigger riêng.<br><br>**Observability**:<br>• **Sentry** — error + performance + tracing, phủ BE NestJS + FE Next + **Mobile Expo** (Sentry Expo SDK), 1 tool cả stack.<br>• **Log** = Pino structured (JSON) → Render logs v1; ship log aggregator (Better Stack/Axiom free) khi cần retention/search.<br>• **Metric** = Render built-in + Sentry performance.<br>• **Trace** = **OpenTelemetry** instrument (vendor-neutral) → export Sentry; swap Grafana Tempo/Datadog không re-instrument.<br>• **Uptime** = free monitor (Better Stack/UptimeRobot); alert = Sentry error spike + uptime.<br>• Full Grafana/Prometheus/Loki stack = **defer** (ops nặng); Datadog/New Relic paid = loại v1. |
| Alternatives đã xét | (1) **Grafana Cloud stack** (Loki log + Prometheus metric + Tempo trace + Grafana dashboard + Sentry errors) — observability đầy đủ nhất, free tier. Yếu: setup + nuôi 3-4 component nặng cho solo nghiệp dư; defer khi có tải thật. (2) **Minimal v1** (chỉ Sentry errors + Render built-in log/metric) — nhẹ nhất. Yếu: ít insight (không perf/trace) — Sentry-centric chỉ thêm chút công cover nhiều hơn. (3) **Datadog / New Relic** (paid APM) — best-in-class. Yếu: $$ mâu thuẫn "miễn phí v1"; overkill MVP. (4) **ELK self-host** (Elasticsearch + Logstash + Kibana) — mạnh nhưng ops cực nặng, RAM tốn; loại cho solo. |
| Hệ quả tích cực | (a) **1 tool Sentry** phủ error + perf + trace cả BE + FE + Mobile → ops thấp solo, giá trị cao nhất. (b) **OTel vendor-neutral** — không lock; bật Grafana/Datadog sau không re-instrument. (c) **GitHub Actions + Render auto-deploy seamless** (ADR-023) — push → CI → merge → deploy. (d) **Free tier** cả Sentry + GitHub Actions + uptime → khớp "miễn phí v1". (e) **Pino structured** fit NestJS, JSON query-able. (f) **Turborepo affected CI** nhanh (chỉ build/test phần đổi, ADR-013). (g) **AI-assist tốt** — GitHub Actions + Sentry + OTel training data dồi. (h) **Mobile crash** Expo → Sentry SDK + EAS source map. |
| Hệ quả tiêu cực & mitigation | (1) **Sentry free tier cap** (event/tháng) → mitigate: sample rate + filter noise event; upgrade paid nếu vượt. (2) **Render logs ephemeral/basic** → mitigate: Pino JSON + ship aggregator (Better Stack/Axiom free) khi cần retention/search. (3) **Metric Render built-in hạn chế** → mitigate: Sentry perf + OTel metric export khi cần Prometheus. (4) **Không full trace stack v1** → mitigate: OTel instrument sẵn → bật Tempo/Grafana sau không re-instrument. (5) **CI/CD secret** (Render deploy hook, Sentry DSN, EAS token) → mitigate: GitHub Actions secrets + Render env group; không commit. (6) **CI flaky/chậm** khi monorepo lớn → mitigate: Turborepo affected + cache + tách e2e (Playwright/Maestro) sang job riêng/scheduled. |
| Nguồn | Sprint 4 Layer 19 workshop (01/06/2026), Sprint 4 closing layer, Phase 4 DevOps; primer (log/metric/trace/error/CI-CD) + CI/CD GitHub Actions + observability 3 option matrix; Khanh chốt "Sentry-centric (Recommended)" + tiêu chí "miễn phí v1". Consistent **ADR-023/024** (Render deploy + worker tách), **ADR-013** (Turborepo CI affected), **ADR-012** (gen api-client CI), **ADR-014** (EAS mobile + Sentry Expo), **ADR-025** (Vitest CI). **Unblock 09 Deploy (full) + 11 Task Breakdown**. **Sprint 4 CLOSED — 4/4 Phase 4 DevOps layer. TOÀN BỘ 19 layer roadmap (15 tech-selection + 4 DevOps) CHỐT XONG.** Còn 2 production-blocker: OQ-21 (KYC storage), OQ-22 (TGTT license). |

---

## 6. Open Questions / TBD

| ID | Câu hỏi | Tác động |
| -- | ------- | -------- |
| ADR-OQ-01 | ADR nào đã được người duyệt chính thức chấp nhận? | Trạng thái ADR |
| ADR-OQ-02 | Có cần tách ADR theo từng file riêng khi số lượng tăng không? | Quản lý tài liệu |
| ADR-OQ-03 | Node/npm/Docker image có cần pin cứng bằng ADR không? | Deployment |
| ADR-OQ-04 | KYC + payment-proof production storage location (VN local split-bucket vs Cloudflare R2 + DPIA Nghị định 13/2023 cross-border) — dev/test dùng local storage adapter; production defer phát triển tiếp | Compliance + KYC module + storage; blocker trước production go-live; track PROJECT-STATE OQ-21 |
| ADR-OQ-05 | Giấy phép trung gian thanh toán NHNN cho production (escrow Platform giữ tiền hành khách + chi cho Operator = hoạt động TGTT theo Nghị định 52/2024) — MVP/sandbox không vướng; production phải có license hoặc đi qua bên licensed; 3 hướng (Platform tự xin / cổng licensed giữ+settle / thu hộ-chi hộ partner licensed) | Compliance + Payment + Escrow module; blocker pháp lý trước production go-live (KHÔNG block MVP); track PROJECT-STATE OQ-22 |
