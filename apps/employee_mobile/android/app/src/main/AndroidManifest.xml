<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Định vị cho Driver (ADR-028). `geolocator` KHÔNG tự khai quyền này — manifest
         của nó chỉ khai `GeolocatorLocationService`, nên quyền phải khai ở đây.
         Đừng khai lại `<service>` ở file này: plugin đã khai rồi, và `.GeolocatorLocationService`
         ở đây sẽ trỏ vào `com.vexenhanh.employee_mobile.…` — một class không tồn tại.
         Đây cũng là điểm phân biệt thật giữa 2 app: `passenger_mobile` không có. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Tắt Android Auto Backup: `flutter_secure_storage` lưu ciphertext trong
         SharedPreferences còn khoá bọc thì nằm trong Keystore, mà Keystore KHÔNG được
         backup. Bật backup ⇒ khôi phục sang máy mới sẽ đọc token và ném
         `InvalidKeyException: Failed to unwrap key`, đồng thời blob token rời khỏi
         thiết bị. README của chính plugin yêu cầu đặt false. -->
    <application
        android:allowBackup="false"
        android:label="employee_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
